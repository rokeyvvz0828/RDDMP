# System 模块规则

职责：组织、用户、角色、菜单、参数和系统管理。公开调用基线见 `governance/modules.yaml`。

- 数据、菜单和权限是平台级业务基础信息，调用方不得复制维护或硬编码替代。
- 角色和菜单变更必须同时验证后端授权、动态路由和前端操作权限。
- 用户、头像和组织变更必须验证数据范围、对象存储访问和审计，不暴露存储凭据。
- 聚焦测试：`mvn -pl :ccb-system -am test`。
