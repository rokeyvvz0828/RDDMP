<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccb.system.internal.capability.SystemOperationAuditMapper">
    <insert id="insert">
        INSERT INTO sys_operation_log
            (id, tenant_id, operator_id, operation_code, request_method, request_path, success, error_message, trace_id)
        VALUES
            (#{id}, #{tenantId}, #{operatorId}, #{operationCode}, #{requestMethod}, #{requestPath}, #{success}, #{errorMessage}, #{traceId})
    </insert>
</mapper>
