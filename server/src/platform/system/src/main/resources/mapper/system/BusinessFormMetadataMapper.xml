<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccb.system.formmetadata.BusinessFormMetadataMapper">
<select id="listScopes" resultType="map">SELECT s.id,s.scope_key,s.scope_name,s.module_key,s.entity_type,s.form_key,s.status_field,s.permission_prefix,s.published_revision_id,s.enabled,COUNT(DISTINCT sec.id) section_count,COUNT(DISTINCT f.id) field_count FROM biz_form_scope s LEFT JOIN biz_form_section sec ON sec.scope_id=s.id AND sec.tenant_id=s.tenant_id AND sec.deleted=0 LEFT JOIN biz_form_field_definition f ON f.scope_id=s.id AND f.tenant_id=s.tenant_id AND f.deleted=0 WHERE s.tenant_id=#{tenantId} AND s.deleted=0 AND (s.scope_key LIKE #{like} OR s.scope_name LIKE #{like} OR s.module_key LIKE #{like}) GROUP BY s.id,s.scope_key,s.scope_name,s.module_key,s.entity_type,s.form_key,s.status_field,s.permission_prefix,s.published_revision_id,s.enabled ORDER BY s.module_key,s.scope_key</select>
<select id="scope" resultType="map">SELECT id,scope_key,scope_name,module_key,entity_type,form_key,status_field,permission_prefix,published_revision_id,enabled FROM biz_form_scope WHERE id=#{scopeId} AND tenant_id=#{tenantId} AND deleted=0</select>
<select id="sections" resultType="map">SELECT id,scope_id,section_key,title,layout_mode,show_title,collapsed,sort_no,is_builtin,enabled FROM biz_form_section WHERE tenant_id=#{tenantId} AND scope_id=#{scopeId} AND deleted=0 ORDER BY sort_no,id</select>
<select id="fields" resultType="map">SELECT id,scope_id,section_id,field_key,label,field_kind,input_type,value_type,source_type,source_key,component_key,native_column,multiple,column_span,visible,list_visible,filterable,sortable,dashboard_dimension,placeholder,help_text,default_value_json,sort_no,is_builtin,enabled FROM biz_form_field_definition WHERE tenant_id=#{tenantId} AND scope_id=#{scopeId} AND deleted=0 ORDER BY sort_no,id</select>
<select id="rules" resultType="map">SELECT id,action_code,condition_type,condition_key,required,editable,visible,validation_json,enabled FROM biz_form_field_rule WHERE tenant_id=#{tenantId} AND field_definition_id=#{fieldId} AND deleted=0 ORDER BY id</select>
<select id="options" resultType="map">SELECT id,option_value,option_label,option_group,sort_no,enabled FROM biz_form_field_option WHERE tenant_id=#{tenantId} AND field_definition_id=#{fieldId} AND deleted=0 ORDER BY sort_no,id</select>
<select id="revisions" resultType="map">SELECT id,revision_no,revision_status,change_summary,created_by,published_by,created_at,published_at FROM biz_form_config_revision WHERE tenant_id=#{tenantId} AND scope_id=#{scopeId} ORDER BY revision_no DESC</select>
<select id="scopeDuplicate" resultType="int">SELECT COUNT(*) FROM biz_form_scope WHERE tenant_id=#{tenantId} AND scope_key=#{scopeKey} AND deleted=0</select>
<insert id="insertScope">INSERT INTO biz_form_scope (id,tenant_id,scope_key,scope_name,module_key,entity_type,form_key,status_field,permission_prefix,enabled,created_by,updated_by) VALUES (#{id},#{tenantId},#{scopeKey},#{scopeName},#{moduleKey},#{entityType},#{formKey},#{statusField},#{permissionPrefix},1,#{operatorId},#{operatorId})</insert>
<update id="updateScope">UPDATE biz_form_scope <set><if test="scopeName != null">scope_name=#{scopeName},</if><if test="moduleKey != null">module_key=#{moduleKey},</if><if test="entityType != null">entity_type=#{entityType},</if><if test="formKey != null">form_key=#{formKey},</if><if test="statusField != null">status_field=#{statusField},</if><if test="permissionPrefix != null">permission_prefix=#{permissionPrefix},</if><if test="enabled != null">enabled=#{enabled},</if>updated_by=#{operatorId}</set> WHERE id=#{scopeId} AND tenant_id=#{tenantId} AND deleted=0</update>
<select id="sectionCount" resultType="int">SELECT COUNT(*) FROM biz_form_section WHERE id=#{sectionId} AND scope_id=#{scopeId} AND tenant_id=#{tenantId} AND deleted=0</select><select id="fieldCountForSection" resultType="int">SELECT COUNT(*) FROM biz_form_field_definition WHERE tenant_id=#{tenantId} AND section_id=#{sectionId} AND deleted=0</select>
<select id="section" resultType="map">SELECT id,scope_id,section_key,title,layout_mode,show_title,collapsed,sort_no,is_builtin,enabled FROM biz_form_section WHERE id=#{sectionId} AND tenant_id=#{tenantId} AND deleted=0</select>
<insert id="insertSection">INSERT INTO biz_form_section (id,tenant_id,scope_id,section_key,title,layout_mode,show_title,collapsed,sort_no,is_builtin,enabled,created_by,updated_by) VALUES (#{id},#{tenantId},#{scopeId},#{sectionKey},#{title},#{layoutMode},#{showTitle},#{collapsed},#{sortNo},0,1,#{operatorId},#{operatorId})</insert>
<update id="updateSection">UPDATE biz_form_section <set><if test="sectionKey != null">section_key=#{sectionKey},</if><if test="title != null">title=#{title},</if><if test="layoutMode != null">layout_mode=#{layoutMode},</if><if test="showTitle != null">show_title=#{showTitle},</if><if test="collapsed != null">collapsed=#{collapsed},</if><if test="sortNo != null">sort_no=#{sortNo},</if><if test="enabled != null">enabled=#{enabled},</if>updated_by=#{operatorId}</set> WHERE id=#{sectionId} AND tenant_id=#{tenantId} AND deleted=0</update>
<update id="deleteSection">UPDATE biz_form_section SET deleted=1,updated_at=CURRENT_TIMESTAMP,updated_by=#{operatorId} WHERE id=#{sectionId} AND tenant_id=#{tenantId}</update>
<select id="fieldCount" resultType="int">SELECT COUNT(*) FROM biz_form_field_definition WHERE id=#{fieldId} AND scope_id=#{scopeId} AND tenant_id=#{tenantId} AND deleted=0</select><select id="fieldDuplicate" resultType="int">SELECT COUNT(*) FROM biz_form_field_definition WHERE tenant_id=#{tenantId} AND scope_id=#{scopeId} AND field_key=#{fieldKey} AND deleted=0</select>
<select id="field" resultType="map">SELECT id,scope_id,section_id,field_key,label,field_kind,input_type,value_type,source_type,source_key,component_key,native_column,multiple,column_span,visible,list_visible,filterable,sortable,dashboard_dimension,placeholder,help_text,default_value_json,sort_no,is_builtin,enabled FROM biz_form_field_definition WHERE id=#{fieldId} AND scope_id=#{scopeId} AND tenant_id=#{tenantId} AND deleted=0</select>
<sql id="fieldColumns">id,tenant_id,scope_id,section_id,field_key,label,field_kind,input_type,value_type,source_type,source_key,component_key,native_column,multiple,column_span,visible,list_visible,filterable,sortable,dashboard_dimension,placeholder,help_text,default_value_json,sort_no,is_builtin,enabled,created_by,updated_by</sql>
<insert id="insertField">INSERT INTO biz_form_field_definition (<include refid="fieldColumns"/>) VALUES (#{id},#{tenantId},#{scopeId},#{sectionId},#{fieldKey},#{label},#{fieldKind},#{inputType},#{valueType},#{sourceType},#{sourceKey},#{componentKey},#{nativeColumn},#{multiple},#{columnSpan},#{visible},#{listVisible},#{filterable},#{sortable},#{dashboardDimension},#{placeholder},#{helpText},#{defaultValueJson},#{sortNo},0,#{enabled},#{operatorId},#{operatorId})</insert>
<update id="updateField">UPDATE biz_form_field_definition <set><if test="sectionId != null">section_id=#{sectionId},</if><if test="fieldKey != null">field_key=#{fieldKey},</if><if test="label != null">label=#{label},</if><if test="fieldKind != null">field_kind=#{fieldKind},</if><if test="inputType != null">input_type=#{inputType},</if><if test="valueType != null">value_type=#{valueType},</if><if test="sourceType != null">source_type=#{sourceType},</if><if test="sourceKey != null">source_key=#{sourceKey},</if><if test="componentKey != null">component_key=#{componentKey},</if><if test="nativeColumn != null">native_column=#{nativeColumn},</if><if test="multiple != null">multiple=#{multiple},</if><if test="columnSpan != null">column_span=#{columnSpan},</if><if test="visible != null">visible=#{visible},</if><if test="listVisible != null">list_visible=#{listVisible},</if><if test="filterable != null">filterable=#{filterable},</if><if test="sortable != null">sortable=#{sortable},</if><if test="dashboardDimension != null">dashboard_dimension=#{dashboardDimension},</if><if test="placeholder != null">placeholder=#{placeholder},</if><if test="helpText != null">help_text=#{helpText},</if><if test="defaultValueJson != null">default_value_json=#{defaultValueJson},</if><if test="sortNo != null">sort_no=#{sortNo},</if><if test="enabled != null">enabled=#{enabled},</if>updated_by=#{operatorId}</set> WHERE id=#{fieldId} AND tenant_id=#{tenantId} AND deleted=0</update>
<update id="deleteField">UPDATE biz_form_field_definition SET deleted=1,updated_at=CURRENT_TIMESTAMP,updated_by=#{operatorId} WHERE id=#{fieldId} AND tenant_id=#{tenantId}</update>
<update id="deleteRules">UPDATE biz_form_field_rule SET deleted=1,updated_at=CURRENT_TIMESTAMP,updated_by=#{operatorId} WHERE tenant_id=#{tenantId} AND field_definition_id=#{fieldId} AND deleted=0</update><insert id="insertRule">INSERT INTO biz_form_field_rule (id,tenant_id,field_definition_id,action_code,condition_type,condition_key,required,editable,visible,validation_json,enabled,created_by,updated_by) VALUES (#{id},#{tenantId},#{fieldId},#{actionCode},#{conditionType},#{conditionKey},#{required},#{editable},#{visible},#{validationJson},1,#{operatorId},#{operatorId})</insert>
<update id="deleteOptions">UPDATE biz_form_field_option SET deleted=1,updated_at=CURRENT_TIMESTAMP,updated_by=#{operatorId} WHERE tenant_id=#{tenantId} AND field_definition_id=#{fieldId} AND deleted=0</update><insert id="insertOption">INSERT INTO biz_form_field_option (id,tenant_id,field_definition_id,option_value,option_label,option_group,sort_no,enabled,created_by,updated_by) VALUES (#{id},#{tenantId},#{fieldId},#{optionValue},#{optionLabel},#{optionGroup},#{sortNo},1,#{operatorId},#{operatorId})</insert>
<select id="nextRevisionNo" resultType="int">SELECT COALESCE(MAX(revision_no),0)+1 FROM biz_form_config_revision WHERE tenant_id=#{tenantId} AND scope_id=#{scopeId}</select><update id="archivePublished">UPDATE biz_form_config_revision SET revision_status='archived' WHERE tenant_id=#{tenantId} AND scope_id=#{scopeId} AND revision_status='published'</update><insert id="insertRevision">INSERT INTO biz_form_config_revision (id,tenant_id,scope_id,revision_no,revision_status,snapshot_json,change_summary,created_by,published_by,published_at) VALUES (#{id},#{tenantId},#{scopeId},#{revisionNo},'published',#{snapshotJson},#{summary},#{operatorId},#{operatorId},CURRENT_TIMESTAMP)</insert><update id="publishScope">UPDATE biz_form_scope SET published_revision_id=#{revisionId},updated_by=#{operatorId},updated_at=CURRENT_TIMESTAMP WHERE id=#{scopeId} AND tenant_id=#{tenantId} AND deleted=0</update>
</mapper>
