# RDDMP 平台模块规则

适用于 `server/src/platform`。平台能力对普通业务任务默认只读。

- 仅平台 Owner、治理任务或已批准的公共能力变更可修改。
- 平台模块不得依赖具体业务模块；`boot` 仅作为组合根装配模块。
- 安全、数据库、迁移、MinIO、启动配置和统一 Web 行为属于高风险变更，必须人工专项复核并执行全量回归。
- 环境配置使用变量或受控密钥管理，不提交真实地址与凭据。
