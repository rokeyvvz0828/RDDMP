/**
 * Workflow application services.
 *
 * <p>Flowable owns process execution and task lifecycle. The service layer projects
 * those engine records into the platform tables, resolves business assignees, and
 * records auditable human actions without exposing engine details to controllers.</p>
 */
package com.ccb.workflow.service;
