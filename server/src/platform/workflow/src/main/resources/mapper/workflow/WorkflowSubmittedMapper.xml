<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccb.workflow.service.WorkflowSubmittedMapper">
    <sql id="from">FROM wf_instance i LEFT JOIN wf_definition d ON d.id = i.definition_id AND d.tenant_id = i.tenant_id LEFT JOIN sys_user starter ON starter.id = i.starter_id AND starter.tenant_id = i.tenant_id</sql>
    <sql id="where">WHERE i.tenant_id = #{tenantId} AND i.starter_id = #{userId} AND i.deleted = 0 <choose><when test="projectIds != null and projectIds.size() > 0">AND (i.project_id IS NULL OR i.project_id IN <foreach collection="projectIds" item="id" open="(" separator="," close=")">#{id}</foreach>)</when><otherwise>AND i.project_id IS NULL</otherwise></choose></sql>
    <sql id="select">SELECT i.id AS instance_id, i.business_key, i.business_type, i.business_title, i.business_round, i.project_ref, i.project_name, i.action_path, i.status AS instance_status, i.created_at, starter.display_name AS starter_name, d.name AS definition_name</sql>
    <select id="page" resultType="map"><include refid="select"/> <include refid="from"/> <include refid="where"/> ORDER BY i.id DESC LIMIT #{offset}, #{size}</select>
    <select id="count" resultType="long">SELECT COUNT(*) <include refid="from"/> <include refid="where"/></select>
    <select id="activeTasks" resultType="map">SELECT t.instance_id, t.id, t.task_key, t.node_id, t.task_type, t.status, COALESCE(t.assignee_name, u.display_name) AS assignee_name, t.assignee_id FROM wf_task t LEFT JOIN sys_user u ON u.id = t.assignee_id AND u.tenant_id = t.tenant_id WHERE t.tenant_id = #{tenantId} AND t.status IN ('PENDING','SENT') AND t.instance_id IN <foreach collection="instanceIds" item="id" open="(" separator="," close=")">#{id}</foreach></select>
</mapper>
