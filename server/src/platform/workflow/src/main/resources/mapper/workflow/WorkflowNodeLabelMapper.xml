<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccb.workflow.service.WorkflowNodeLabelMapper">
    <select id="definitionJson" resultType="string">
        SELECT CAST(v.definition_json AS CHAR)
        FROM wf_instance i
        JOIN wf_version v ON v.definition_id = i.definition_id
            AND v.tenant_id = i.tenant_id AND v.version_no = i.version_no
        WHERE i.id = #{instanceId} AND i.tenant_id = #{tenantId}
    </select>
</mapper>
