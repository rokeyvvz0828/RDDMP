<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccb.workflow.service.WorkflowDefinitionSummaryMapper">
    <update id="refresh">
        UPDATE wf_definition
        SET latest_version_no = #{versionNo}, requires_configuration = #{requiresConfiguration}
        WHERE id = #{definitionId} AND tenant_id = #{tenantId} AND deleted = 0
    </update>
</mapper>
