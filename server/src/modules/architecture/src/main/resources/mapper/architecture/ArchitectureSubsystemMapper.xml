<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.ccb.architecture.repository.ArchitectureSubsystemMapper"><sql id="cols">id,code,short_name,name,logical_subsystem_name,business_component_code,business_group_name,deployment_platform,disaster_recovery_mode,responsible_team_org_id,responsible_team_name_snapshot,runtime_code,system_level_code,development_framework_code,owner_user_id,description,remark,created_by,updated_by,created_at,updated_at,english_name,status,row_version</sql><sql id="filter">WHERE tenant_id=#{tenantId} AND deleted=0<if test="code != null"> AND code LIKE #{code} ESCAPE '\\'</if><if test="shortName != null"> AND short_name LIKE #{shortName} ESCAPE '\\'</if><if test="name != null"> AND name LIKE #{name} ESCAPE '\\'</if><if test="logicalSubsystemName != null"> AND logical_subsystem_name LIKE #{logicalSubsystemName} ESCAPE '\\'</if><if test="businessComponentCode != null"> AND business_component_code=#{businessComponentCode}</if><if test="businessGroupName != null"> AND business_group_name LIKE #{businessGroupName} ESCAPE '\\'</if><if test="responsibleTeamOrgId != null"> AND responsible_team_org_id=#{responsibleTeamOrgId}</if><if test="status != null"> AND status=#{status}</if></sql><select id="countPhysical" resultType="long">SELECT COUNT(*) FROM arch_physical_subsystem <include refid="filter"/></select><select id="physicals" resultType="map">SELECT <include refid="cols"/> FROM arch_physical_subsystem <include refid="filter"/> ORDER BY id DESC LIMIT #{size} OFFSET #{offset}</select><select id="physical" resultType="map">SELECT <include refid="cols"/> FROM arch_physical_subsystem WHERE tenant_id=#{tenantId} AND id=#{id} AND deleted=0</select><select id="countByCode" resultType="long">SELECT COUNT(*) FROM arch_physical_subsystem WHERE tenant_id=#{tenantId} AND code=#{value}<if test="excludeId != null"> AND id &lt;&gt; #{excludeId}</if></select><select id="countByName" resultType="long">SELECT COUNT(*) FROM arch_physical_subsystem WHERE tenant_id=#{tenantId} AND name=#{value}<if test="excludeId != null"> AND id &lt;&gt; #{excludeId}</if></select><insert id="insertPhysical">INSERT INTO arch_physical_subsystem (id,tenant_id,code,short_name,name,logical_subsystem_name,business_component_code,business_group_name,deployment_platform,disaster_recovery_mode,responsible_team_org_id,responsible_team_name_snapshot,runtime_code,system_level_code,development_framework_code,owner_user_id,description,remark,created_by,updated_by) VALUES (#{id},#{tenantId},#{code},#{shortName},#{name},#{logicalSubsystemName},#{businessComponentCode},#{businessGroupName},#{deploymentPlatform},#{disasterRecoveryMode},#{responsibleTeamOrgId},#{responsibleTeamNameSnapshot},#{runtimeCode},#{systemLevelCode},#{developmentFrameworkCode},#{ownerUserId},#{description},#{remark},#{actorId},#{actorId})</insert><update id="updatePhysical">UPDATE arch_physical_subsystem SET code=#{code},short_name=#{shortName},name=#{name},logical_subsystem_name=#{logicalSubsystemName},business_component_code=#{businessComponentCode},business_group_name=#{businessGroupName},deployment_platform=#{deploymentPlatform},disaster_recovery_mode=#{disasterRecoveryMode},responsible_team_org_id=#{responsibleTeamOrgId},responsible_team_name_snapshot=#{responsibleTeamNameSnapshot},runtime_code=#{runtimeCode},system_level_code=#{systemLevelCode},development_framework_code=#{developmentFrameworkCode},owner_user_id=#{ownerUserId},description=#{description},remark=#{remark},updated_by=#{actorId} WHERE tenant_id=#{tenantId} AND id=#{id} AND deleted=0</update><update id="softDeletePhysical">UPDATE arch_physical_subsystem SET deleted=1,updated_by=#{actorId} WHERE tenant_id=#{tenantId} AND id=#{id} AND deleted=0</update></mapper>
