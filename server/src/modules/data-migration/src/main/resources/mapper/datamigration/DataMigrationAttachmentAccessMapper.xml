<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccb.datamigration.integration.DataMigrationAttachmentAccessMapper">
  <select id="meeting" resultType="map">SELECT created_by,deleted FROM dm_meeting WHERE meeting_id=#{id} AND tenant_id=#{tenantId}</select>
  <select id="assets" resultType="map"><foreach collection="tables" item="entry" separator=" UNION ALL ">SELECT '${entry.type}' AS asset_type,owner_id,deleted FROM ${entry.table} WHERE id=#{id} AND tenant_id=#{tenantId}</foreach></select>
</mapper>
