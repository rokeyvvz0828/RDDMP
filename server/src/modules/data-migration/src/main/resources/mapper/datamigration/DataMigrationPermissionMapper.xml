<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccb.datamigration.service.DataMigrationPermissionMapper">
  <select id="adminPermissionCount" resultType="int">SELECT COUNT(*) FROM sys_user_role ur JOIN sys_role r ON r.id = ur.role_id AND r.tenant_id = ur.tenant_id JOIN sys_role_permission rp ON rp.role_id = r.id AND rp.tenant_id = r.tenant_id JOIN sys_menu_permission p ON p.id = rp.permission_id AND p.tenant_id = rp.tenant_id WHERE ur.user_id = #{userId} AND ur.tenant_id = #{tenantId} AND p.permission_code IN ('system:admin', 'data-migration:bypass', 'data-migration:manage') AND p.status = 1 AND r.status = 1 AND r.deleted = 0</select>
  <select id="actionPermissionCount" resultType="int">SELECT COUNT(*) FROM sys_menu_permission p JOIN sys_role_permission rp ON rp.permission_id = p.id AND rp.tenant_id = p.tenant_id JOIN sys_user_role ur ON ur.role_id = rp.role_id AND ur.tenant_id = rp.tenant_id JOIN sys_role r ON r.id = ur.role_id AND r.tenant_id = ur.tenant_id WHERE ur.user_id = #{userId} AND p.tenant_id = #{tenantId} AND p.permission_code = #{permissionCode} AND p.action_code = #{action} AND p.status = 1 AND r.status = 1 AND r.deleted = 0</select>
</mapper>
