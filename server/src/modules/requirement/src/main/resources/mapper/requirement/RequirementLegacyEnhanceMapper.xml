<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccb.requirement.service.RequirementLegacyEnhanceMapper">
 <sql id="deliverableColumns">d.id,d.requirement_id,d.system_item_id,d.system_code,d.doc_name,d.version_no,d.review_status,d.review_record_id,d.file_preview_id,d.remark,d.review_approver_ids,d.review_approver_names,d.review_report_name,d.created_at,d.updated_at,r.remark review_remark</sql>
 <select id="deliverables" resultType="map">SELECT <include refid="deliverableColumns"/> FROM <choose><when test="type == 'WORKLOAD'">req_workload</when><otherwise>req_soft_doc</otherwise></choose> d LEFT JOIN req_review_record r ON r.id=d.review_record_id AND r.deleted=0 WHERE d.tenant_id=#{tenantId} AND d.requirement_id=#{requirementId} AND d.deleted=0 ORDER BY d.system_item_id,d.version_no DESC,d.id DESC</select>
 <insert id="insertDeliverable">INSERT INTO <choose><when test="type == 'WORKLOAD'">req_workload</when><otherwise>req_soft_doc</otherwise></choose> (<foreach collection="p" item="v" index="k" separator=",">${k}</foreach>) VALUES (<foreach collection="p" item="v" index="k" separator=",">#{v}</foreach>)</insert>
 <update id="deleteDeliverable">UPDATE <choose><when test="type == 'WORKLOAD'">req_workload</when><otherwise>req_soft_doc</otherwise></choose> SET deleted=1 WHERE tenant_id=#{tenantId} AND id=#{id}</update>
 <update id="submitReview">UPDATE <choose><when test="type == 'WORKLOAD'">req_workload</when><otherwise>req_soft_doc</otherwise></choose> SET review_status='评审中',review_approver_ids=#{ids},review_approver_names=#{names},review_report_name=#{report} WHERE tenant_id=#{tenantId} AND id=#{id}</update>
 <update id="reviewDeliverable">UPDATE <choose><when test="type == 'WORKLOAD'">req_workload</when><otherwise>req_soft_doc</otherwise></choose> SET review_status=#{status},review_record_id=#{recordId} WHERE tenant_id=#{tenantId} AND id=#{id}</update>
 <select id="coordinationItems" resultType="map">SELECT id,system_item_id,item_type,system_code,system_name,owner_user_id,owner_user_name,start_date,end_date,status,description,created_at FROM req_coordination_item WHERE tenant_id=#{tenantId} AND requirement_id=#{requirementId} AND deleted=0 ORDER BY id</select>
 <insert id="insertCoordination">INSERT INTO req_coordination_item (<foreach collection="p" item="v" index="k" separator=",">${k}</foreach>) VALUES (<foreach collection="p" item="v" index="k" separator=",">#{v}</foreach>)</insert>
 <update id="updateCoordination">UPDATE req_coordination_item <set><foreach collection="p" item="v" index="k" separator=",">${k}=#{v}</foreach></set> WHERE tenant_id=#{p.tenant_id} AND id=#{p.id}</update>
 <update id="deleteCoordination">UPDATE req_coordination_item SET deleted=1 WHERE tenant_id=#{tenantId} AND id=#{id}</update>
 <select id="reviewRecords" resultType="map">SELECT id,biz_type,biz_id,review_no,reviewer_id,reviewer_name,review_time,conclusion,comment,remark,report_doc_name,created_at FROM req_review_record WHERE tenant_id=#{tenantId} AND biz_type=#{bizType} AND biz_id=#{bizId} AND deleted=0 ORDER BY created_at DESC,id DESC</select>
 <insert id="insertReviewRecord">INSERT INTO req_review_record (id,tenant_id,biz_type,biz_id,reviewer_id,reviewer_name,review_time,conclusion,comment,remark,report_doc_name,created_by,deleted) VALUES (#{p.id},#{p.tenant_id},#{p.biz_type},#{p.biz_id},#{p.reviewer_id},#{p.reviewer_name},CURRENT_TIMESTAMP,#{p.conclusion},#{p.comment},#{p.remark},#{p.report_doc_name},#{p.created_by},0)</insert>
 <select id="approverNames" resultType="map">SELECT id,COALESCE(display_name,username) name FROM sys_user WHERE tenant_id=#{tenantId} AND id IN <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach> AND deleted=0</select>
 <select id="legacyRequirement" resultType="map">SELECT created_by,current_flow_user_id FROM req_legacy_requirement WHERE tenant_id=#{tenantId} AND id=#{id} AND deleted=0</select>
 <select id="versions" resultType="string">SELECT version_no FROM <choose><when test="type == 'WORKLOAD'">req_workload</when><otherwise>req_soft_doc</otherwise></choose> WHERE tenant_id=#{tenantId} AND requirement_id=#{requirementId} <if test="systemItemId != null">AND system_item_id=#{systemItemId}</if><if test="systemItemId == null and systemCode != null">AND system_code=#{systemCode}</if> AND deleted=0</select>
 <select id="deliverable" resultType="map">SELECT <include refid="deliverableColumns"/> FROM <choose><when test="type == 'WORKLOAD'">req_workload</when><otherwise>req_soft_doc</otherwise></choose> d LEFT JOIN req_review_record r ON r.id=d.review_record_id AND r.deleted=0 WHERE d.tenant_id=#{tenantId} AND d.id=#{id} AND d.deleted=0</select>
 <select id="coordination" resultType="map">SELECT id,requirement_id,system_item_id,item_type,system_code,system_name,owner_user_id,owner_user_name,start_date,end_date,status,description,created_at FROM req_coordination_item WHERE tenant_id=#{tenantId} AND id=#{id} AND deleted=0</select>
</mapper>
