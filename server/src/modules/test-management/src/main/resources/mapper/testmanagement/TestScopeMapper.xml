<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccb.testmanagement.scope.TestScopeMapper">
  <sql id="scopeStatus">CASE WHEN s.invalidated=1 THEN 'INVALID' WHEN NOT EXISTS (SELECT 1 FROM tm_test_case c0 WHERE c0.tenant_id=s.tenant_id AND c0.scope_id=s.id AND c0.deleted=0 AND c0.invalidated=0) THEN 'INVALID' WHEN EXISTS (SELECT 1 FROM tm_test_case c1 JOIN tm_test_execution e1 ON e1.case_id=c1.id AND e1.tenant_id=c1.tenant_id AND e1.deleted=0 WHERE c1.tenant_id=s.tenant_id AND c1.scope_id=s.id AND c1.deleted=0 AND c1.invalidated=0 AND e1.execution_status='FAILED') THEN 'FAILED' WHEN EXISTS (SELECT 1 FROM tm_test_case c2 JOIN tm_test_execution e2 ON e2.case_id=c2.id AND e2.tenant_id=c2.tenant_id AND e2.deleted=0 WHERE c2.tenant_id=s.tenant_id AND c2.scope_id=s.id AND c2.deleted=0 AND c2.invalidated=0 AND e2.execution_status='RUNNING') THEN 'RUNNING' WHEN EXISTS (SELECT 1 FROM tm_test_case c3 WHERE c3.tenant_id=s.tenant_id AND c3.scope_id=s.id AND c3.deleted=0 AND c3.invalidated=0 AND NOT EXISTS (SELECT 1 FROM tm_test_execution e3 WHERE e3.tenant_id=c3.tenant_id AND e3.case_id=c3.id AND e3.deleted=0 AND e3.execution_status='SUCCESS')) THEN 'UNEXECUTED' ELSE 'SUCCESS' END</sql>
  <sql id="caseCount">(SELECT COUNT(*) FROM tm_test_case c WHERE c.tenant_id=s.tenant_id AND c.scope_id=s.id AND c.deleted=0 AND c.invalidated=0)</sql>
  <sql id="scopeFilter">WHERE s.tenant_id=#{tenantId} AND s.test_domain=#{domain} AND s.project_id=#{projectId} AND s.deleted=#{deleted}<if test="systemId != null"> AND s.physical_subsystem_id=#{systemId}</if><if test="directoryIds != null and !directoryIds.isEmpty()"> AND s.directory_id IN <foreach collection="directoryIds" item="value" open="(" separator="," close=")">#{value}</foreach></if><if test="keyword != null"> AND (s.scope_code LIKE #{keyword} OR s.scope_name LIKE #{keyword} OR s.leaf_menu LIKE #{keyword})</if><if test="functionTypes != null and !functionTypes.isEmpty()"> AND s.function_type IN <foreach collection="functionTypes" item="value" open="(" separator="," close=")">#{value}</foreach></if><if test="changeStatuses != null and !changeStatuses.isEmpty()"> AND s.change_status IN <foreach collection="changeStatuses" item="value" open="(" separator="," close=")">#{value}</foreach></if><if test="importances != null and !importances.isEmpty()"> AND s.importance IN <foreach collection="importances" item="value" open="(" separator="," close=")">#{value}</foreach></if><if test="accountingFlags != null and !accountingFlags.isEmpty()"> AND s.accounting_flag IN <foreach collection="accountingFlags" item="value" open="(" separator="," close=")">#{value}</foreach></if><if test="statuses != null and !statuses.isEmpty()"> AND <include refid="scopeStatus"/> IN <foreach collection="statuses" item="value" open="(" separator="," close=")">#{value}</foreach></if><if test="coverage == 'COVERED'"> AND <include refid="caseCount"/> &gt; 0</if><if test="coverage == 'UNCOVERED'"> AND <include refid="caseCount"/> = 0</if></sql>

  <select id="treeSystems" resultType="map">SELECT s.physical_subsystem_id AS id,p.code,p.short_name,COALESCE(NULLIF(p.short_name,''),REPLACE(p.name,'物理子系统','')) AS name FROM tm_test_participating_system s JOIN arch_physical_subsystem p ON p.id=s.physical_subsystem_id AND p.tenant_id=s.tenant_id AND p.deleted=0 WHERE s.tenant_id=#{tenantId} AND s.test_domain=#{domain} AND s.project_id=#{projectId} AND s.enabled=1 AND s.deleted=0 ORDER BY p.code,p.id</select>
  <select id="treeDirectories" resultType="map">SELECT d.id,d.physical_subsystem_id,d.parent_id,d.directory_name,d.sort_no,(SELECT COUNT(*) FROM tm_test_scope x WHERE x.tenant_id=d.tenant_id AND x.directory_id=d.id AND x.deleted=0) AS scope_count FROM tm_test_scope_directory d WHERE d.tenant_id=#{tenantId} AND d.test_domain=#{domain} AND d.project_id=#{projectId} AND d.deleted=0 ORDER BY d.physical_subsystem_id,d.sort_no,d.id</select>
  <select id="scopeCount" resultType="long">SELECT COUNT(*) FROM tm_test_scope s <include refid="scopeFilter"/></select>
  <select id="scopes" resultType="map">SELECT s.id,s.scope_code,s.scope_name,s.leaf_menu,s.physical_subsystem_id,p.code AS physical_system_code,COALESCE(NULLIF(p.short_name,''),REPLACE(p.name,'物理子系统','')) AS physical_system_name,s.directory_id,d.directory_name,s.function_type,s.change_status,s.importance,s.accounting_flag,s.invalidated,s.invalid_reason,s.deleted,s.deleted_at,s.created_by,creator.display_name AS created_by_name,s.created_at,s.updated_by,updater.display_name AS updated_by_name,s.updated_at,<include refid="caseCount"/> AS case_count,<include refid="scopeStatus"/> AS status FROM tm_test_scope s JOIN arch_physical_subsystem p ON p.id=s.physical_subsystem_id AND p.tenant_id=s.tenant_id LEFT JOIN tm_test_scope_directory d ON d.id=s.directory_id AND d.tenant_id=s.tenant_id AND d.deleted=0 LEFT JOIN sys_user creator ON creator.id=s.created_by AND creator.tenant_id=s.tenant_id AND creator.deleted=0 LEFT JOIN sys_user updater ON updater.id=s.updated_by AND updater.tenant_id=s.tenant_id AND updater.deleted=0 <include refid="scopeFilter"/> ORDER BY <choose><when test="sortBy == 'scope_code'">s.scope_code</when><when test="sortBy == 'scope_name'">s.scope_name</when><when test="sortBy == 'physical_system_name'">p.name</when><when test="sortBy == 'directory_name'">d.directory_name</when><when test="sortBy == 'case_count'"><include refid="caseCount"/></when><when test="sortBy == 'created_at'">s.created_at</when><otherwise>s.updated_at</otherwise></choose><choose><when test="ascending"> ASC</when><otherwise> DESC</otherwise></choose>,s.id DESC LIMIT #{offset},#{size}</select>
  <select id="projectCount" resultType="long">SELECT COUNT(*) FROM pm_project WHERE id=#{projectId} AND tenant_id=#{tenantId} AND deleted=0</select>
  <select id="participatingSystemCount" resultType="long">SELECT COUNT(*) FROM tm_test_participating_system WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND physical_subsystem_id=#{systemId} AND enabled=1 AND deleted=0</select>
  <select id="systemByReference" resultType="map">SELECT p.id FROM tm_test_participating_system s JOIN arch_physical_subsystem p ON p.id=s.physical_subsystem_id AND p.tenant_id=s.tenant_id AND p.deleted=0 WHERE s.tenant_id=#{tenantId} AND s.test_domain=#{domain} AND s.project_id=#{projectId} AND s.enabled=1 AND s.deleted=0 AND (p.code=#{value} OR p.short_name=#{value} OR p.name=#{value})</select>
  <select id="directory" resultType="map">SELECT id,physical_subsystem_id,parent_id,directory_name FROM tm_test_scope_directory WHERE id=#{id} AND tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND deleted=0</select>
  <select id="directoryParent" resultType="map">SELECT parent_id,directory_name FROM tm_test_scope_directory WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</select>
  <select id="childDirectoryIds" resultType="long">SELECT id FROM tm_test_scope_directory WHERE tenant_id=#{tenantId} AND parent_id=#{parentId} AND deleted=0</select>
  <select id="directoryNameCount" resultType="long">SELECT COUNT(*) FROM tm_test_scope_directory WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND physical_subsystem_id=#{systemId}<choose><when test="parentId == null"> AND parent_id IS NULL</when><otherwise> AND parent_id=#{parentId}</otherwise></choose> AND directory_name=#{name} AND deleted=0<if test="id != null"> AND id&lt;&gt;#{id}</if></select>
  <select id="directoryByParentAndName" resultType="map">SELECT id FROM tm_test_scope_directory WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND physical_subsystem_id=#{systemId}<choose><when test="parentId == null"> AND parent_id IS NULL</when><otherwise> AND parent_id=#{parentId}</otherwise></choose> AND directory_name=#{name} AND deleted=0</select>
  <insert id="insertDirectory">INSERT INTO tm_test_scope_directory(id,tenant_id,test_domain,project_id,physical_subsystem_id,parent_id,directory_name,sort_no,created_by,updated_by) VALUES(#{id},#{tenantId},#{domain},#{projectId},#{systemId},#{parentId},#{name},#{sortNo},#{userId},#{userId})</insert>
  <update id="updateDirectory">UPDATE tm_test_scope_directory SET physical_subsystem_id=#{systemId},parent_id=#{parentId},directory_name=#{name},sort_no=#{sortNo},updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</update>
  <select id="directoryResult" resultType="map">SELECT id,physical_subsystem_id,parent_id,directory_name,sort_no,updated_at FROM tm_test_scope_directory WHERE id=#{id} AND tenant_id=#{tenantId}</select>
  <select id="childDirectoryCount" resultType="long">SELECT COUNT(*) FROM tm_test_scope_directory WHERE tenant_id=#{tenantId} AND parent_id=#{id} AND deleted=0</select>
  <update id="reassignScopeDirectory">UPDATE tm_test_scope SET directory_id=#{targetDirectoryId},updated_by=#{userId} WHERE tenant_id=#{tenantId} AND directory_id=#{directoryId} AND deleted=0</update>
  <update id="deleteDirectory">UPDATE tm_test_scope_directory SET deleted=1,deleted_at=CURRENT_TIMESTAMP,updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</update>
  <select id="scopeRow" resultType="map">SELECT id,scope_code,physical_subsystem_id,directory_id FROM tm_test_scope WHERE id=#{id} AND tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND deleted=0</select>
  <select id="deletedScopeRow" resultType="map">SELECT id,scope_code,physical_subsystem_id FROM tm_test_scope WHERE id=#{id} AND tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND deleted=1</select>
  <select id="caseCount" resultType="long">SELECT COUNT(*) FROM tm_test_case WHERE tenant_id=#{tenantId} AND scope_id=#{scopeId} AND deleted=0</select>
  <select id="nextScopeSequence" resultType="int">SELECT COALESCE(MAX(CAST(SUBSTRING_INDEX(scope_code,'-',-1) AS UNSIGNED)),0)+1 FROM tm_test_scope WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND physical_subsystem_id=#{systemId}</select>
  <select id="systemCode" resultType="string">SELECT code FROM arch_physical_subsystem WHERE id=#{systemId} AND tenant_id=#{tenantId} AND deleted=0</select>
  <select id="scopeCodeCount" resultType="long">SELECT COUNT(*) FROM tm_test_scope WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND scope_code=#{scopeCode}<if test="id != null"> AND id&lt;&gt;#{id}</if></select>
  <insert id="insertScope">INSERT INTO tm_test_scope(id,tenant_id,test_domain,project_id,physical_subsystem_id,directory_id,scope_code,scope_name,leaf_menu,function_type,change_status,importance,accounting_flag,created_by,updated_by) VALUES(#{id},#{tenantId},#{domain},#{projectId},#{systemId},#{directoryId},#{scopeCode},#{scopeName},#{leafMenu},#{functionType},#{changeStatus},#{importance},#{accountingFlag},#{userId},#{userId})</insert>
  <update id="updateScope">UPDATE tm_test_scope SET physical_subsystem_id=#{systemId},directory_id=#{directoryId},scope_code=#{scopeCode},scope_name=#{scopeName},leaf_menu=#{leafMenu},function_type=#{functionType},change_status=#{changeStatus},importance=#{importance},accounting_flag=#{accountingFlag},updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</update>
  <update id="syncCaseCodes">UPDATE tm_test_case SET case_code=CONCAT(#{scopeCode}, '-', LPAD(case_serial_no,4,'0')),updated_by=#{userId} WHERE tenant_id=#{tenantId} AND scope_id=#{scopeId} AND deleted=0</update>
  <select id="scopeResult" resultType="map">SELECT id,physical_subsystem_id,directory_id,scope_code,scope_name,leaf_menu,function_type,change_status,importance,accounting_flag,invalidated,updated_at FROM tm_test_scope WHERE id=#{id} AND tenant_id=#{tenantId}</select>
  <update id="invalidateScope">UPDATE tm_test_scope SET invalidated=#{invalidated},invalidated_by=#{invalidatedBy},invalidated_at=#{invalidatedAt},invalid_reason=#{reason},updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId}</update>
  <update id="deleteScope">UPDATE tm_test_scope SET deleted=1,deleted_at=CURRENT_TIMESTAMP,updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</update>
  <update id="restoreScope">UPDATE tm_test_scope SET deleted=0,deleted_at=NULL,scope_code=#{scopeCode},updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId}</update>
  <select id="dictionaryOptionCount" resultType="long">SELECT COUNT(*) FROM tm_test_dictionary d JOIN tm_test_dictionary_option o ON o.dictionary_id=d.id AND o.tenant_id=d.tenant_id AND o.deleted=0 AND o.enabled=1 WHERE d.tenant_id=#{tenantId} AND d.test_domain=#{domain} AND d.project_id=#{projectId} AND d.dictionary_code=#{dictionaryCode} AND d.deleted=0 AND d.enabled=1 AND (o.option_code=#{value} OR o.option_name=#{value})</select>
  <select id="activeScopeByCode" resultType="map">SELECT id FROM tm_test_scope WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND scope_code=#{scopeCode} AND deleted=0</select>
  <insert id="insertAudit">INSERT INTO tm_test_scope_case_audit(id,tenant_id,test_domain,project_id,entity_type,entity_id,action_code,operator_id,detail_json) VALUES(#{id},#{tenantId},#{domain},#{projectId},#{entityType},#{entityId},#{actionCode},#{userId},#{detailJson})</insert>
</mapper>
