<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccb.testmanagement.casework.TestCaseMapper">
  <sql id="caseStatus">CASE WHEN c.invalidated=1 THEN 'INVALID' ELSE COALESCE((SELECT e.execution_status FROM tm_test_execution e WHERE e.tenant_id=c.tenant_id AND e.case_id=c.id AND e.deleted=0 ORDER BY COALESCE(e.executed_at,e.created_at) DESC,e.updated_at DESC,e.id DESC LIMIT 1),'UNEXECUTED') END</sql>
  <sql id="caseFilter">WHERE c.tenant_id=#{tenantId} AND c.test_domain=#{domain} AND c.project_id=#{projectId} AND c.deleted=0<if test="systemId != null"> AND c.physical_subsystem_id=#{systemId}</if><if test="directoryIds != null and !directoryIds.isEmpty()"> AND c.directory_id IN <foreach collection="directoryIds" item="id" open="(" separator="," close=")">#{id}</foreach></if><if test="scopeId != null"> AND c.scope_id=#{scopeId}</if><if test="keyword != null"> AND (c.case_code LIKE #{keyword} OR c.case_name LIKE #{keyword} OR s.scope_code LIKE #{keyword} OR s.scope_name LIKE #{keyword})</if><if test="caseTypes != null and !caseTypes.isEmpty()"> AND c.case_type IN <foreach collection="caseTypes" item="value" open="(" separator="," close=")">#{value}</foreach></if><if test="caseNatures != null and !caseNatures.isEmpty()"> AND c.test_level IN <foreach collection="caseNatures" item="value" open="(" separator="," close=")">#{value}</foreach></if><if test="priorities != null and !priorities.isEmpty()"> AND c.priority IN <foreach collection="priorities" item="value" open="(" separator="," close=")">#{value}</foreach></if><if test="accountingResults != null and !accountingResults.isEmpty()"> AND c.accounting_result IN <foreach collection="accountingResults" item="value" open="(" separator="," close=")">#{value}</foreach></if><if test="designerId != null"> AND c.created_by=#{designerId}</if><if test="statuses != null and !statuses.isEmpty()"> AND <include refid="caseStatus"/> IN <foreach collection="statuses" item="value" open="(" separator="," close=")">#{value}</foreach></if><if test="executionReference == 'REFERENCED'"> AND EXISTS (SELECT 1 FROM tm_test_execution e0 WHERE e0.tenant_id=c.tenant_id AND e0.case_id=c.id AND e0.deleted=0)</if><if test="executionReference == 'UNREFERENCED'"> AND NOT EXISTS (SELECT 1 FROM tm_test_execution e0 WHERE e0.tenant_id=c.tenant_id AND e0.case_id=c.id AND e0.deleted=0)</if></sql>
  <select id="treeSystems" resultType="map">SELECT s.physical_subsystem_id AS id,p.code,p.short_name,COALESCE(NULLIF(p.short_name,''),REPLACE(p.name,'物理子系统','')) AS name FROM tm_test_participating_system s JOIN arch_physical_subsystem p ON p.id=s.physical_subsystem_id AND p.tenant_id=s.tenant_id AND p.deleted=0 WHERE s.tenant_id=#{tenantId} AND s.test_domain=#{domain} AND s.project_id=#{projectId} AND s.enabled=1 AND s.deleted=0 ORDER BY p.code,p.id</select>
  <select id="treeDirectories" resultType="map">SELECT d.id,d.physical_subsystem_id,d.parent_id,d.directory_name,d.sort_no,(SELECT COUNT(*) FROM tm_test_case c WHERE c.tenant_id=d.tenant_id AND c.directory_id=d.id AND c.deleted=0) AS case_count FROM tm_test_case_directory d WHERE d.tenant_id=#{tenantId} AND d.test_domain=#{domain} AND d.project_id=#{projectId} AND d.deleted=0 ORDER BY d.physical_subsystem_id,d.sort_no,d.id</select>
  <select id="scopes" resultType="map">SELECT s.id,s.scope_code,s.scope_name,s.physical_subsystem_id,p.code AS physical_system_code,COALESCE(NULLIF(p.short_name,''),REPLACE(p.name,'物理子系统','')) AS physical_system_name FROM tm_test_scope s JOIN arch_physical_subsystem p ON p.id=s.physical_subsystem_id AND p.tenant_id=s.tenant_id AND p.deleted=0 WHERE s.tenant_id=#{tenantId} AND s.test_domain=#{domain} AND s.project_id=#{projectId} AND s.deleted=0<if test="systemId != null"> AND s.physical_subsystem_id=#{systemId}</if> ORDER BY s.scope_code</select>
  <select id="caseCount" resultType="long">SELECT COUNT(*) FROM tm_test_case c JOIN tm_test_scope s ON s.id=c.scope_id AND s.tenant_id=c.tenant_id AND s.deleted=0 <include refid="caseFilter"/></select>
  <select id="casePage" resultType="map">SELECT c.id,c.case_code,c.case_name,c.physical_subsystem_id,c.scope_id,s.scope_code,s.scope_name,p.code AS physical_system_code,COALESCE(NULLIF(p.short_name,''),REPLACE(p.name,'物理子系统','')) AS physical_system_name,c.directory_id,d.directory_name,c.case_type,c.test_level AS case_nature,c.priority,c.invalidated,c.invalid_reason,c.accounting_result,c.accounting_confirmed,c.accounting_confirmed_by,confirmer.display_name AS accounting_confirmer_name,c.created_by,creator.display_name AS created_by_name,c.created_at,c.updated_by,updater.display_name AS updated_by_name,c.updated_at,<include refid="caseStatus"/> AS status,(SELECT COUNT(*) FROM tm_test_case_attachment a WHERE a.tenant_id=c.tenant_id AND a.case_id=c.id AND a.deleted=0) AS attachment_count,(SELECT COUNT(*) FROM tm_test_execution e0 WHERE e0.tenant_id=c.tenant_id AND e0.case_id=c.id AND e0.deleted=0) AS execution_reference_count FROM tm_test_case c JOIN tm_test_scope s ON s.id=c.scope_id AND s.tenant_id=c.tenant_id AND s.deleted=0 JOIN arch_physical_subsystem p ON p.id=c.physical_subsystem_id AND p.tenant_id=c.tenant_id AND p.deleted=0 LEFT JOIN tm_test_case_directory d ON d.id=c.directory_id AND d.tenant_id=c.tenant_id AND d.deleted=0 LEFT JOIN sys_user creator ON creator.id=c.created_by AND creator.tenant_id=c.tenant_id AND creator.deleted=0 LEFT JOIN sys_user updater ON updater.id=c.updated_by AND updater.tenant_id=c.tenant_id AND updater.deleted=0 LEFT JOIN sys_user confirmer ON confirmer.id=c.accounting_confirmed_by AND confirmer.tenant_id=c.tenant_id AND confirmer.deleted=0 <include refid="caseFilter"/> ORDER BY <choose><when test="sortBy == 'case_code'">c.case_code</when><when test="sortBy == 'case_name'">c.case_name</when><when test="sortBy == 'scope_code'">s.scope_code</when><when test="sortBy == 'case_type'">c.case_type</when><when test="sortBy == 'case_nature'">c.test_level</when><when test="sortBy == 'priority'">c.priority</when><when test="sortBy == 'accounting_result'">c.accounting_result</when><when test="sortBy == 'created_at'">c.created_at</when><otherwise>c.updated_at</otherwise></choose><choose><when test="ascending"> ASC</when><otherwise> DESC</otherwise></choose>,c.id DESC LIMIT #{limit} OFFSET #{offset}</select>
  <select id="caseRow" resultType="map">SELECT * FROM tm_test_case WHERE id=#{id} AND tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND deleted=0</select>
  <select id="attachments" resultType="map">SELECT a.attachment_id AS id,f.file_name,f.file_size,f.content_type,a.sort_no FROM tm_test_case_attachment a LEFT JOIN att_file f ON f.id=a.attachment_id AND f.tenant_id=a.tenant_id WHERE a.tenant_id=#{tenantId} AND a.case_id=#{caseId} AND a.deleted=0 ORDER BY a.sort_no,a.id</select>
  <select id="attachmentRows" resultType="map">SELECT attachment_id FROM tm_test_case_attachment WHERE tenant_id=#{tenantId} AND case_id=#{caseId} AND deleted=0</select>
  <select id="attachmentIds" resultType="long">SELECT attachment_id FROM tm_test_case_attachment WHERE tenant_id=#{tenantId} AND case_id=#{caseId} AND deleted=0</select>
  <select id="directoryNameCount" resultType="long">SELECT COUNT(*) FROM tm_test_case_directory WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND physical_subsystem_id=#{systemId}<choose><when test="parentId == null"> AND parent_id IS NULL</when><otherwise> AND parent_id=#{parentId}</otherwise></choose> AND directory_name=#{name} AND deleted=0<if test="id != null"> AND id&lt;&gt;#{id}</if></select>
  <insert id="insertDirectory">INSERT INTO tm_test_case_directory(id,tenant_id,test_domain,project_id,physical_subsystem_id,parent_id,directory_name,sort_no,created_by,updated_by) VALUES(#{id},#{tenantId},#{domain},#{projectId},#{systemId},#{parentId},#{name},#{sortNo},#{userId},#{userId})</insert>
  <update id="updateDirectory">UPDATE tm_test_case_directory SET physical_subsystem_id=#{systemId},parent_id=#{parentId},directory_name=#{name},sort_no=#{sortNo},updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</update>
  <select id="directorySaved" resultType="map">SELECT id,physical_subsystem_id,parent_id,directory_name,sort_no,updated_at FROM tm_test_case_directory WHERE id=#{id} AND tenant_id=#{tenantId}</select>
  <select id="childDirectoryCount" resultType="long">SELECT COUNT(*) FROM tm_test_case_directory WHERE tenant_id=#{tenantId} AND parent_id=#{id} AND deleted=0</select>
  <update id="moveDirectoryCases">UPDATE tm_test_case SET directory_id=#{targetDirectoryId},updated_by=#{userId} WHERE tenant_id=#{tenantId} AND directory_id=#{id} AND deleted=0</update>
  <update id="deleteDirectory">UPDATE tm_test_case_directory SET deleted=1,deleted_at=CURRENT_TIMESTAMP,updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</update>
  <insert id="insertCase">INSERT INTO tm_test_case(id,tenant_id,test_domain,project_id,physical_subsystem_id,scope_id,directory_id,case_code,case_serial_no,case_name,case_type,test_level,priority,accounting_result,accounting_confirmed,accounting_confirmed_by,accounting_confirmed_at,precondition_html,steps_html,expected_result_html,remark,created_by,updated_by) VALUES(#{id},#{tenantId},#{domain},#{projectId},#{systemId},#{scopeId},#{directoryId},#{caseCode},#{serial},#{name},#{caseType},#{caseNature},#{priority},#{accountingResult},#{accountingConfirmed},#{accountingConfirmedBy},#{accountingConfirmedAt},#{preconditionHtml},#{stepsHtml},#{expectedResultHtml},#{remark},#{userId},#{userId})</insert>
  <update id="updateCase">UPDATE tm_test_case SET physical_subsystem_id=#{systemId},scope_id=#{scopeId},directory_id=#{directoryId},case_code=#{caseCode},case_serial_no=#{serial},case_name=#{name},case_type=#{caseType},test_level=#{caseNature},priority=#{priority},accounting_result=#{accountingResult},accounting_confirmed=#{accountingConfirmed},accounting_confirmed_by=#{accountingConfirmedBy},accounting_confirmed_at=#{accountingConfirmedAt},precondition_html=#{preconditionHtml},steps_html=#{stepsHtml},expected_result_html=#{expectedResultHtml},remark=#{remark},updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</update>
  <update id="moveCase">UPDATE tm_test_case SET directory_id=#{targetDirectoryId},updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId}</update>
  <update id="invalidateCase">UPDATE tm_test_case SET invalidated=#{invalidated},invalidated_by=#{invalidatedBy},invalidated_at=#{invalidatedAt},invalid_reason=#{invalidReason},updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId}</update>
  <update id="deleteCase">UPDATE tm_test_case SET deleted=1,deleted_at=CURRENT_TIMESTAMP,updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId}</update>
  <select id="caseByCode" resultType="map">SELECT id FROM tm_test_case WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND case_code=#{caseCode} AND deleted=0</select>
  <select id="importCaseByCode" resultType="map">SELECT id,deleted FROM tm_test_case WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND case_code=#{caseCode}</select>
  <update id="deleteAttachment">UPDATE tm_test_case_attachment SET deleted=1,deleted_at=CURRENT_TIMESTAMP WHERE tenant_id=#{tenantId} AND case_id=#{caseId}<if test="attachmentId != null"> AND attachment_id=#{attachmentId}</if></update>
  <select id="attachmentExists" resultType="long">SELECT COUNT(*) FROM tm_test_case_attachment WHERE tenant_id=#{tenantId} AND case_id=#{caseId} AND attachment_id=#{attachmentId} AND deleted=0</select>
  <insert id="insertAttachment">INSERT INTO tm_test_case_attachment(id,tenant_id,case_id,attachment_id,sort_no,created_by) VALUES(#{id},#{tenantId},#{caseId},#{attachmentId},#{sortNo},#{userId})</insert>
  <select id="projectExists" resultType="long">SELECT COUNT(*) FROM pm_project WHERE id=#{projectId} AND tenant_id=#{tenantId} AND deleted=0</select>
  <select id="enabledSystemExists" resultType="long">SELECT COUNT(*) FROM tm_test_participating_system WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND physical_subsystem_id=#{systemId} AND enabled=1 AND deleted=0</select>
  <select id="scope" resultType="map">SELECT id,scope_code,physical_subsystem_id FROM tm_test_scope WHERE id=#{id} AND tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND deleted=0</select>
  <select id="scopeByCode" resultType="map">SELECT id,scope_code,physical_subsystem_id FROM tm_test_scope WHERE scope_code=#{scopeCode} AND tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND deleted=0</select>
  <select id="directory" resultType="map">SELECT id,physical_subsystem_id,parent_id,directory_name FROM tm_test_case_directory WHERE id=#{id} AND tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND deleted=0</select>
  <select id="dictionaryOption" resultType="string">SELECT o.option_code FROM tm_test_dictionary d JOIN tm_test_dictionary_option o ON o.dictionary_id=d.id AND o.tenant_id=d.tenant_id AND o.deleted=0 AND o.enabled=1 WHERE d.tenant_id=#{tenantId} AND d.test_domain=#{domain} AND d.project_id=#{projectId} AND d.dictionary_code=#{dictionaryCode} AND d.deleted=0 AND d.enabled=1 AND (o.option_code=#{value} OR o.option_name=#{value})</select>
  <select id="activeUserExists" resultType="long">SELECT COUNT(*) FROM sys_user WHERE id=#{userId} AND tenant_id=#{tenantId} AND deleted=0 AND status=1</select>
  <select id="directoryParent" resultType="map">SELECT parent_id FROM tm_test_case_directory WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</select>
  <select id="directoryChildren" resultType="long">SELECT id FROM tm_test_case_directory WHERE tenant_id=#{tenantId} AND parent_id=#{id} AND deleted=0</select>
  <select id="nextSerial" resultType="long">SELECT COALESCE(MAX(case_serial_no),0)+1 FROM tm_test_case WHERE tenant_id=#{tenantId} AND scope_id=#{scopeId}</select>
  <select id="serialExists" resultType="long">SELECT COUNT(*) FROM tm_test_case WHERE tenant_id=#{tenantId} AND scope_id=#{scopeId} AND case_serial_no=#{serial} AND id&lt;&gt;#{id}</select>
  <select id="directoryByName" resultType="map">SELECT id FROM tm_test_case_directory WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND physical_subsystem_id=#{systemId}<choose><when test="parentId == null"> AND parent_id IS NULL</when><otherwise> AND parent_id=#{parentId}</otherwise></choose> AND directory_name=#{name} AND deleted=0</select>
  <select id="directoryPath" resultType="map">SELECT parent_id,directory_name FROM tm_test_case_directory WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</select>
  <insert id="insertAudit">INSERT INTO tm_test_scope_case_audit(id,tenant_id,test_domain,project_id,entity_type,entity_id,action_code,operator_id,detail_json) VALUES(#{id},#{tenantId},#{domain},#{projectId},#{type},#{entityId},#{action},#{userId},#{detailJson})</insert>
</mapper>
