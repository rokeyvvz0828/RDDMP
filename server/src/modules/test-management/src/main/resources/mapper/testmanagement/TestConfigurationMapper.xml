<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccb.testmanagement.service.TestConfigurationMapper">
  <sql id="systemFilter"><where>p.tenant_id=#{tenantId} AND p.deleted=0<if test="keyword != null and keyword != ''"> AND (p.code LIKE CONCAT('%',#{keyword},'%') OR p.short_name LIKE CONCAT('%',#{keyword},'%') OR p.name LIKE CONCAT('%',#{keyword},'%'))</if></where></sql>
  <select id="projectOptions" resultType="map">SELECT id,project_code,project_name,status FROM pm_project WHERE tenant_id=#{tenantId} AND deleted=0 ORDER BY project_name,id</select>
  <select id="physicalSystemCount" resultType="long">SELECT COUNT(*) FROM arch_physical_subsystem p <include refid="systemFilter"/></select>
  <select id="physicalSystemPage" resultType="map">SELECT p.id AS physical_subsystem_id,p.code,p.short_name,COALESCE(NULLIF(p.short_name,''),REPLACE(p.name,'物理子系统','')) AS name,COALESCE(s.enabled,0) AS enabled,s.remark,s.updated_at FROM arch_physical_subsystem p LEFT JOIN tm_test_participating_system s ON s.tenant_id=p.tenant_id AND s.test_domain=#{domain} AND s.project_id=#{projectId} AND s.physical_subsystem_id=p.id AND s.deleted=0 <include refid="systemFilter"/> ORDER BY p.code,p.id LIMIT #{offset},#{size}</select>
  <select id="participatingSystemId" resultType="map">SELECT id FROM tm_test_participating_system WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND physical_subsystem_id=#{physicalId} AND deleted=0</select>
  <insert id="insertParticipatingSystem">INSERT INTO tm_test_participating_system(id,tenant_id,test_domain,project_id,physical_subsystem_id,enabled,remark,created_by,updated_by) VALUES(#{id},#{tenantId},#{domain},#{projectId},#{physicalId},#{enabled},#{remark},#{userId},#{userId})</insert>
  <update id="updateParticipatingSystem">UPDATE tm_test_participating_system SET enabled=#{enabled},remark=#{remark},updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</update>
  <select id="participatingSystem" resultType="map">SELECT id,physical_subsystem_id,enabled,remark,updated_at FROM tm_test_participating_system WHERE id=#{id} AND tenant_id=#{tenantId}</select>
  <select id="roleCount" resultType="long">SELECT COUNT(*) FROM tm_test_system_role WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND physical_subsystem_id=#{physicalId} AND deleted=0</select>
  <select id="rolePage" resultType="map">SELECT id,user_id,role_code,role_name,created_at FROM tm_test_system_role WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND physical_subsystem_id=#{physicalId} AND deleted=0 ORDER BY role_name,id LIMIT #{offset},#{size}</select>
  <select id="roleExists" resultType="long">SELECT COUNT(*) FROM tm_test_system_role WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND physical_subsystem_id=#{physicalId} AND user_id=#{memberId} AND role_code=#{roleCode} AND deleted=0</select>
  <insert id="insertRole">INSERT INTO tm_test_system_role(id,tenant_id,test_domain,project_id,physical_subsystem_id,user_id,role_code,role_name,created_by,updated_by) VALUES(#{id},#{tenantId},#{domain},#{projectId},#{physicalId},#{memberId},#{roleCode},#{roleName},#{userId},#{userId})</insert>
  <select id="role" resultType="map">SELECT id,project_id,user_id FROM tm_test_system_role WHERE id=#{id} AND tenant_id=#{tenantId} AND test_domain=#{domain} AND deleted=0</select>
  <update id="deleteRole">UPDATE tm_test_system_role SET deleted=1,updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</update>
  <select id="projectExists" resultType="long">SELECT COUNT(*) FROM pm_project WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</select>
  <select id="physicalExists" resultType="long">SELECT COUNT(*) FROM arch_physical_subsystem WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</select>
  <select id="enabledParticipatingSystemExists" resultType="long">SELECT COUNT(*) FROM tm_test_participating_system WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND physical_subsystem_id=#{physicalId} AND enabled=1 AND deleted=0</select>
  <select id="roundExists" resultType="long">SELECT COUNT(*) FROM tm_test_round WHERE id=#{id} AND tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND deleted=0</select>
  <select id="cycleExists" resultType="long">SELECT COUNT(*) FROM tm_test_cycle WHERE id=#{id} AND round_id=#{roundId} AND tenant_id=#{tenantId} AND deleted=0</select>
  <select id="dictionaryExists" resultType="long">SELECT COUNT(*) FROM tm_test_dictionary WHERE id=#{id} AND tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND deleted=0</select>
  <select id="optionExists" resultType="long">SELECT COUNT(*) FROM tm_test_dictionary_option WHERE id=#{id} AND dictionary_id=#{dictionaryId} AND tenant_id=#{tenantId} AND deleted=0</select>
  <select id="roundCount" resultType="long">SELECT COUNT(*) FROM tm_test_round WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND deleted=0</select>
  <select id="roundPage" resultType="map">SELECT id,round_code,round_name,planned_start_date,planned_end_date,status,sort_no,remark,updated_at FROM tm_test_round WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND deleted=0 ORDER BY sort_no,id LIMIT #{offset},#{size}</select>
  <select id="roundUniqueCodeCount" resultType="long">SELECT COUNT(*) FROM tm_test_round WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND round_code=#{value} AND deleted=0<if test="exceptId != null"> AND id != #{exceptId}</if></select>
  <select id="roundUniqueNameCount" resultType="long">SELECT COUNT(*) FROM tm_test_round WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND round_name=#{value} AND deleted=0<if test="exceptId != null"> AND id != #{exceptId}</if></select>
  <insert id="insertRound">INSERT INTO tm_test_round(id,tenant_id,test_domain,project_id,round_code,round_name,planned_start_date,planned_end_date,status,sort_no,remark,created_by,updated_by) VALUES(#{id},#{tenantId},#{domain},#{projectId},#{roundCode},#{roundName},#{startDate},#{endDate},#{status},#{sortNo},#{remark},#{userId},#{userId})</insert>
  <update id="updateRound">UPDATE tm_test_round SET round_code=#{roundCode},round_name=#{roundName},planned_start_date=#{startDate},planned_end_date=#{endDate},status=#{status},sort_no=#{sortNo},remark=#{remark},updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</update>
  <select id="round" resultType="map">SELECT id,round_code,round_name,planned_start_date,planned_end_date,status,sort_no,remark,updated_at FROM tm_test_round WHERE id=#{id} AND tenant_id=#{tenantId}</select>
  <select id="cycleReferenceCount" resultType="long">SELECT COUNT(*) FROM tm_test_cycle WHERE tenant_id=#{tenantId} AND round_id=#{roundId} AND deleted=0</select>
  <update id="deleteRound">UPDATE tm_test_round SET deleted=1,updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId}</update>
  <select id="cycleCount" resultType="long">SELECT COUNT(*) FROM tm_test_cycle WHERE tenant_id=#{tenantId} AND round_id=#{roundId} AND deleted=0</select>
  <select id="cyclePage" resultType="map">SELECT id,cycle_code,cycle_name,planned_start_date,planned_end_date,status,sort_no,remark,updated_at FROM tm_test_cycle WHERE tenant_id=#{tenantId} AND round_id=#{roundId} AND deleted=0 ORDER BY sort_no,id LIMIT #{offset},#{size}</select>
  <select id="cycleUniqueCodeCount" resultType="long">SELECT COUNT(*) FROM tm_test_cycle WHERE tenant_id=#{tenantId} AND round_id=#{roundId} AND cycle_code=#{value} AND deleted=0<if test="exceptId != null"> AND id != #{exceptId}</if></select>
  <select id="cycleUniqueNameCount" resultType="long">SELECT COUNT(*) FROM tm_test_cycle WHERE tenant_id=#{tenantId} AND round_id=#{roundId} AND cycle_name=#{value} AND deleted=0<if test="exceptId != null"> AND id != #{exceptId}</if></select>
  <insert id="insertCycle">INSERT INTO tm_test_cycle(id,tenant_id,round_id,cycle_code,cycle_name,planned_start_date,planned_end_date,status,sort_no,remark,created_by,updated_by) VALUES(#{id},#{tenantId},#{roundId},#{cycleCode},#{cycleName},#{startDate},#{endDate},#{status},#{sortNo},#{remark},#{userId},#{userId})</insert>
  <update id="updateCycle">UPDATE tm_test_cycle SET cycle_code=#{cycleCode},cycle_name=#{cycleName},planned_start_date=#{startDate},planned_end_date=#{endDate},status=#{status},sort_no=#{sortNo},remark=#{remark},updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</update>
  <select id="cycle" resultType="map">SELECT id,cycle_code,cycle_name,planned_start_date,planned_end_date,status,sort_no,remark,updated_at FROM tm_test_cycle WHERE id=#{id} AND tenant_id=#{tenantId}</select>
  <update id="deleteCycle">UPDATE tm_test_cycle SET deleted=1,updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId}</update>
  <select id="dictionaryCount" resultType="long">SELECT COUNT(*) FROM tm_test_dictionary WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND deleted=0</select>
  <select id="dictionaryPage" resultType="map">SELECT id,dictionary_code,dictionary_name,source_type,enabled,remark,updated_at FROM tm_test_dictionary WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND deleted=0 ORDER BY dictionary_name,id LIMIT #{offset},#{size}</select>
  <select id="dictionaryUniqueCodeCount" resultType="long">SELECT COUNT(*) FROM tm_test_dictionary WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND dictionary_code=#{value} AND deleted=0<if test="exceptId != null"> AND id != #{exceptId}</if></select>
  <insert id="insertDictionary">INSERT INTO tm_test_dictionary(id,tenant_id,test_domain,project_id,dictionary_code,dictionary_name,source_type,enabled,remark,created_by,updated_by) VALUES(#{id},#{tenantId},#{domain},#{projectId},#{dictionaryCode},#{dictionaryName},#{sourceType},#{enabled},#{remark},#{userId},#{userId})</insert>
  <update id="updateDictionary">UPDATE tm_test_dictionary SET dictionary_code=#{dictionaryCode},dictionary_name=#{dictionaryName},enabled=#{enabled},remark=#{remark},updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</update>
  <select id="dictionary" resultType="map">SELECT id,dictionary_code,dictionary_name,source_type,enabled,remark,updated_at FROM tm_test_dictionary WHERE id=#{id} AND tenant_id=#{tenantId}</select>
  <select id="dictionaryOptionReferenceCount" resultType="long">SELECT COUNT(*) FROM tm_test_dictionary_option WHERE tenant_id=#{tenantId} AND dictionary_id=#{dictionaryId} AND deleted=0</select>
  <update id="deleteDictionary">UPDATE tm_test_dictionary SET deleted=1,updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</update>
  <select id="optionCount" resultType="long">SELECT COUNT(*) FROM tm_test_dictionary_option WHERE tenant_id=#{tenantId} AND dictionary_id=#{dictionaryId} AND deleted=0</select>
  <select id="optionPage" resultType="map">SELECT id,option_code,option_name,enabled,sort_no,remark,updated_at FROM tm_test_dictionary_option WHERE tenant_id=#{tenantId} AND dictionary_id=#{dictionaryId} AND deleted=0 ORDER BY sort_no,id LIMIT #{offset},#{size}</select>
  <select id="optionUniqueCodeCount" resultType="long">SELECT COUNT(*) FROM tm_test_dictionary_option WHERE tenant_id=#{tenantId} AND dictionary_id=#{dictionaryId} AND option_code=#{value} AND deleted=0<if test="exceptId != null"> AND id != #{exceptId}</if></select>
  <insert id="insertOption">INSERT INTO tm_test_dictionary_option(id,tenant_id,dictionary_id,option_code,option_name,enabled,sort_no,remark,created_by,updated_by) VALUES(#{id},#{tenantId},#{dictionaryId},#{optionCode},#{optionName},#{enabled},#{sortNo},#{remark},#{userId},#{userId})</insert>
  <update id="updateOption">UPDATE tm_test_dictionary_option SET option_code=#{optionCode},option_name=#{optionName},enabled=#{enabled},sort_no=#{sortNo},remark=#{remark},updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</update>
  <select id="option" resultType="map">SELECT id,option_code,option_name,enabled,sort_no,remark,updated_at FROM tm_test_dictionary_option WHERE id=#{id} AND tenant_id=#{tenantId}</select>
  <update id="deleteOption">UPDATE tm_test_dictionary_option SET deleted=1,updated_by=#{userId} WHERE id=#{id} AND tenant_id=#{tenantId}</update>
  <select id="roundDates" resultType="map">SELECT planned_start_date,planned_end_date FROM tm_test_round WHERE id=#{id} AND tenant_id=#{tenantId}</select>
  <select id="dictionaryByCode" resultType="map">SELECT id FROM tm_test_dictionary WHERE tenant_id=#{tenantId} AND test_domain=#{domain} AND project_id=#{projectId} AND dictionary_code=#{dictionaryCode} AND deleted=0</select>
  <select id="physicalByCode" resultType="map">SELECT id FROM arch_physical_subsystem WHERE tenant_id=#{tenantId} AND code=#{code} AND deleted=0</select>
  <insert id="insertAudit">INSERT INTO tm_test_configuration_audit(id,tenant_id,test_domain,project_id,entity_type,entity_id,action_code,operator_id,detail_json) VALUES(#{id},#{tenantId},#{domain},#{projectId},#{entityType},#{entityId},#{action},#{userId},#{detailJson})</insert>
</mapper>
