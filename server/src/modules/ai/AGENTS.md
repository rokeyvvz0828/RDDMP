# AI 模块规则

职责：AI 提供商、模型、能力路由与执行服务。公开调用基线见 `governance/modules.yaml`。

- 业务模块按能力调用，不绑定具体模型提供商，不从浏览器提交或持久化提供商密钥。
- 模型配置、调用输入输出、失败原因和 trace ID 按数据分级审计，日志不得泄露密钥或敏感正文。
- 外部模型访问必须使用受控白名单、超时、失败降级和服务端权限校验。
- 聚焦测试：`mvn -pl :ccb-ai -am test`。
