<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccb.release.window.persistence.ReleaseWindowMapper">
<resultMap id="window" type="com.ccb.release.window.model.ReleaseWindow"><constructor><idArg column="id" javaType="long"/><arg column="tenant_id" javaType="long"/><arg column="window_code"/><arg column="window_name"/><arg column="project_id"/><arg column="project_code"/><arg column="project_name"/><arg column="declaration_start" javaType="java.time.LocalDateTime"/><arg column="declaration_end" javaType="java.time.LocalDateTime"/><arg column="production_start" javaType="java.time.LocalDateTime"/><arg column="production_end" javaType="java.time.LocalDateTime"/><arg column="regular_enabled" javaType="boolean"/><arg column="description"/><arg column="row_version" javaType="long"/><arg column="created_by" javaType="long"/><arg column="updated_by" javaType="long"/><arg column="created_at" javaType="java.time.LocalDateTime"/><arg column="updated_at" javaType="java.time.LocalDateTime"/></constructor></resultMap>
<sql id="columns">id,tenant_id,window_code,window_name,project_id,project_code,project_name,declaration_start,declaration_end,production_start,production_end,regular_enabled,description,row_version,created_by,updated_by,created_at,updated_at</sql>
<sql id="filter">WHERE tenant_id=#{tenantId} AND deleted=0<if test="projectId != null"> AND project_id=#{projectId}</if><if test="keyword != null"> AND (window_code LIKE CONCAT('%',#{keyword},'%') OR window_name LIKE CONCAT('%',#{keyword},'%'))</if></sql>
<select id="count" resultType="long">SELECT COUNT(*) FROM rel_release_window <include refid="filter"/></select>
<select id="page" resultMap="window">SELECT <include refid="columns"/> FROM rel_release_window <include refid="filter"/> ORDER BY production_start DESC,id DESC LIMIT #{size} OFFSET #{offset}</select>
<select id="find" resultMap="window">SELECT <include refid="columns"/> FROM rel_release_window WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0</select>
<select id="findForUpdate" resultMap="window">SELECT <include refid="columns"/> FROM rel_release_window WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0 FOR UPDATE</select>
<select id="tenantId" resultType="long">SELECT tenant_id FROM rel_release_window WHERE id=#{id} AND deleted=0</select>
<select id="lockProjectWindows" resultType="long">SELECT id FROM rel_release_window WHERE tenant_id=#{tenantId} AND project_id=#{projectId} AND deleted=0 ORDER BY declaration_start FOR UPDATE</select>
<select id="overlapCount" resultType="long">SELECT COUNT(*) FROM rel_release_window WHERE tenant_id=#{tenantId} AND project_id=#{projectId} AND deleted=0 AND declaration_start &lt; #{end} AND production_end &gt; #{start}<if test="excludedId != null"> AND id &lt;&gt; #{excludedId}</if></select>
<select id="latestCode" resultType="string">SELECT window_code FROM rel_release_window WHERE tenant_id=#{tenantId} AND window_code LIKE CONCAT(#{monthPrefix},'%') ORDER BY window_code DESC LIMIT 1 FOR UPDATE</select>
<insert id="insert">INSERT INTO rel_release_window (id,tenant_id,window_code,window_name,project_id,project_code,project_name,declaration_start,declaration_end,production_start,production_end,regular_enabled,description,row_version,created_by,updated_by) VALUES (#{id},#{tenantId},#{windowCode},#{windowName},#{projectId},#{projectCode},#{projectName},#{declarationStart},#{declarationEnd},#{productionStart},#{productionEnd},#{regularEnabled},#{description},0,#{createdBy},#{updatedBy})</insert>
<update id="update">UPDATE rel_release_window SET window_name=#{windowName},declaration_start=#{declarationStart},declaration_end=#{declarationEnd},production_start=#{productionStart},production_end=#{productionEnd},regular_enabled=#{regularEnabled},description=#{description},updated_by=#{updatedBy},row_version=row_version+1 WHERE id=#{id} AND tenant_id=#{tenantId} AND deleted=0 AND row_version=#{expectedVersion}</update>
<insert id="appendChange">INSERT INTO rel_window_change_log (id,tenant_id,window_id,field_name,old_value,new_value,change_reason,operator_id) VALUES (#{id},#{tenantId},#{windowId},#{fieldName},#{oldValue},#{newValue},#{reason},#{operatorId})</insert>
</mapper>
