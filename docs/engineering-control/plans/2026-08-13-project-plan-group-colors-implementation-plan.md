# 项目计划分组配色实施计划

## T1 数据与服务端契约

- 追加 V41 `color_key` 字段及索引/注释。
- 更新 Mock 白名单。
- 更新 `ProjectService` 的分组查询、创建、更新和单条读取，校验颜色白名单并默认 ocean。

## T2 前端分组配色

- 扩展分组类型和表单。
- 复用系统主题选项完成新建/编辑选择。
- 为分组行和所属计划行注入颜色属性及语义样式。

## T3 验证与观测

- 执行迁移、治理、类型检查、构建和 diff 检查。
- 对颜色保存、刷新兼容、非法颜色和深色主题样式进行源代码/运行验证；受环境阻断的检查记录限制。
