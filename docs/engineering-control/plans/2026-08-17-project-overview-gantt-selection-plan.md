# 项目概览甘特图选择交互实施计划

## T1：布局与选择驱动

- 在 `ProjectView.vue` 增加选中主计划状态、选中主计划计算属性、主计划图点击监听和按主计划筛选的子计划行。
- 将子计划图改为选择后渲染，补充未选择和无子计划状态。
- 在 `styles.css` 中统一项目概览为清晰的单列排期布局，排期区域上下分层，保留图表局部滚动。

## 验证

- `npm --prefix web run build`
- `git diff --check`
- 浏览器点击两个不同主计划，确认下方子计划名称随选择变化；刷新确认回到未选择引导；检查无页面横向溢出。
