# 项目概览与计划布局实施计划

## T1 数据库与服务端编号契约

- 新增 V39，增加项目子计划规则和计划子序号字段，保留中文注释。
- 扩展 `ProjectService` 的规则校验、渲染、设置返回和服务端生成逻辑。
- 增加主计划与子计划编号测试，确认客户端编号不能覆盖服务端结果。

## T2 项目概览与规则说明界面

- 移除概览中的事实、近期计划和成员面板，仅保留说明和日历横向布局。
- 在项目设置中展示主计划规则、子计划规则、占位符说明和示例。
- 更新类型定义和保存请求。

## T3 固定列与集成验证

- 为 Element Plus 固定列补充不透明主题背景、层级和阴影。
- 运行 Maven 测试、Vite 构建和 `git diff --check`，检查桌面与窄屏布局。
