# 投产方案时序图视觉实施计划

## T1 前端数据与模板

- 在 `ReleaseDrillPlanView.vue` 增加按 `seqNo` 归并的阶段视图模型。
- 将时序抽屉中的普通时间线替换为阶段列、列内指令和底部时间轴。
- 保留现有名称保存、指令新增/编辑/删除和权限显隐。

## T2 样式与响应式

- 在 `release-operations.css` 增加阶段列图形样式。
- 复用语义主题变量，控制阶段宽度、文字换行、局部横向滚动和移动端操作可达性。

## T3 验证与证据

- 执行前端构建、`git diff --check`。
- 在正向/回退、多条指令、空数据、只读权限以及 `375/390/430/1280` 视口下验证。
- 如发现布局偏差，仅回到 T1/T2 修正，不扩展后端范围。
