# 项目组织架构实施计划

1. 追加 V50 迁移，创建项目组织树表、成员机构字段和索引，并为历史成员保留空机构兼容值。
2. 在 `ProjectService` 和 `ProjectController` 增加组织树查询/维护、成员机构保存、角色成员关联接口及服务端校验。
3. 扩展项目 TypeScript 类型和 API，改造项目组织架构页签为左树右维护区，接入组织节点维护和角色人员多选。
4. 执行后端测试、前端类型检查与构建、Flyway 检查、差异检查，并检查服务端接口契约和移动端布局。
