# 配置管理角色菜单权限实施计划

1. 新增 V45，拆分父目录、六个子菜单、动作归属和存量角色授权，并用迁移契约测试固定关键 SQL。
2. 拆分投产基线/生产版本后端权限，补充 Controller 权限测试；扩展投产窗口列表的只读依赖权限。
3. 移除前端强制注入菜单，给认证 store 增加权限和路由判断，给六个静态路由增加权限守卫。
4. 让配置管理模块按当前路由切换有权页签，按权限加载依赖数据；给窗口、申请、基线和审批流程写操作增加显隐控制。
5. 执行聚焦测试、发布模块测试、前端构建、治理/范围/diff 检查，应用 V45 后用四类角色完成桌面与移动浏览器验收。
