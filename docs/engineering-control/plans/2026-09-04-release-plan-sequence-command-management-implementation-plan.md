# 投产方案时序与指令分层实施计划

## T1 数据与服务契约

- 增加时序节点模型、请求模型和嵌套方案返回结构。
- 增加 V129 迁移并保留历史数据兼容。
- 增加时序节点 CRUD、嵌套指令 CRUD 和安全/服务测试。

## T2 前端维护界面

- 使用 `timelines` 替换临时按指令分组。
- 为 P 节点增加新增、编辑、删除操作，为每个 P 节点增加指令新增、编辑、删除操作。
- 删除底部时间线，保留时序名称和现有状态/权限行为。

## T3 验收

- 执行投产模块测试、前端构建、差异检查。
- 在正向/回退、多指令、空数据、只读权限和 `375/390/430/1280` 视口下浏览器验收。
- 检查迁移只追加、范围一致、无新增控制台错误和页面级横向溢出。

## 合并修复补充

- 解决 `origin/main` 与 licon 投产迁移共用 `V123-V129` 导致的 Flyway 重复版本；将 licon 投产迁移顺延为 `V149-V155`，保留原执行顺序和 SQL 内容。
- 在隔离临时目录按 `web/package-lock.json` 安装依赖，确认 `@wangeditor/editor` 缺失仅为本地依赖目录不完整，不修改依赖声明。
