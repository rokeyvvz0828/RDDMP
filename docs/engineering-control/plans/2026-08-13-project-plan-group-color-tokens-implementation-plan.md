# 项目计划分组主题色阶实施计划

## T1 服务端和数据兼容

- 追加 V42，把旧主题编码归一为 `brand`。
- 修改 `ProjectService` 的色阶白名单、读写默认值和非法值校验。

## T2 前端主题联动

- 扩展项目类型为语义色阶。
- 分组表单显示当前主题 CSS 变量的色阶预览。
- 计划表行通过语义色阶类引用主题变量，不再写死颜色。

## T3 验证

- 执行迁移、类型、构建、编译和 diff 检查。
- 记录完整后端测试的环境结果和主题联动静态证据。
