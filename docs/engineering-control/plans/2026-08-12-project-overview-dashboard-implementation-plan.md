# 项目概览计划看板实施计划

## T1 前端数据和图表

- 在 `ProjectView.vue` 中从 `selectedProject.plans` 派生计划状态、进度和排期数据。
- 引入已有 ECharts 依赖，创建、更新、resize 和销毁图表实例。
- 处理无计划、无排期和长计划名称。

## T2 概览布局和样式

- 重构概览为项目摘要、KPI、统计图、说明/日历四个区域。
- 复用主题 CSS 变量，提供桌面、平板和移动端断点。

## T3 验证

- 执行 Vue 类型检查、前端生产构建和 `git diff --check`。
- 记录环境阻断和未进行真实浏览器验收的残余风险。
