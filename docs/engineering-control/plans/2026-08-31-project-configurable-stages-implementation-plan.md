# 项目级可配置计划阶段实施计划

1. 追加 V83，创建项目阶段表并为存量项目初始化默认阶段。
2. 在 `ProjectService` 中增加阶段查询、创建、更新、删除、排序和主计划锁定校验；创建项目时初始化默认阶段；计划和阶段计划改用项目阶段校验。
3. 在 `ProjectController`、项目 API 类型和 `ProjectView` 中接入阶段配置与项目阶段时间轴。
4. 增加服务测试，执行 Maven、前端构建、Flyway、治理和差异检查。
5. 使用本地运行服务验证默认阶段、无主计划可修改、有主计划拒绝修改/删除，并记录运行风险。
