# REQ-20260911-079 设计

工作台先执行现有项目列表查询并保留 `projectScope(user)`。当结果非空时，收集项目 ID 和所有者 ID，分别执行一次租户绑定的所有者批量查询和项目统计批量查询，再按 ID 在内存中组装既有字段。统计查询使用项目集合和相关子查询，避免成员与计划连接产生笛卡尔放大；详情接口继续使用原有单项目装饰逻辑。空列表不执行批量查询。权限、排序、审计、REST 响应和数据库结构不变。
