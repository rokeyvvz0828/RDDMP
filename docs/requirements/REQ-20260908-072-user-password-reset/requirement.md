# REQ-20260908-072 编辑用户重置密码

状态：ready

管理员编辑用户时，填写非空密码并保存后，后端必须使用 BCrypt 重新生成 `password_hash`；留空时保持原密码。密码不得出现在查询响应、审计内容或日志中。保存仍沿用用户更新权限和租户实体范围。

验收：非空密码被编码并写入当前租户目标用户；空密码不修改密码；编辑界面明确提示留空不修改；系统模块测试和前端构建通过。
