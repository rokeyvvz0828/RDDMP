# REQ-20260908-071 Flyway V156 兼容归一化

状态：ready

将标准迁移链中的 V156 固定为平台操作审计迁移，将投产方案菜单重命名顺延为 V200。提供受控兼容工具，仅在识别到已知旧 V156 菜单迁移且数据库已具备审计结构时，将历史记录归一化为标准 V156；已登记的本地兼容 V200 仅在其结构效果已确认且标准迁移尚未执行时移除历史记录，再由标准 V200 幂等执行菜单命名。

验收：标准目录无重复版本；空库迁移可解析；main V156 历史、licon V156 历史和当前本地 V200 历史都有明确且可拒绝未知状态的兼容路径；不修改业务数据与投产表。
