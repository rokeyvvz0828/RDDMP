# 页面改版验证记录

## 实际改动

仅修改产品文件 `web/src/modules/release/components/ReleaseDrillPlanView.vue`，以及本需求范围、设计、计划和前缀账本。

方案目录改为窄列、详情改为宽列；补齐状态/版本呈现，正向与回退使用独立入口；时序抽屉去除重复标题，P 编号和多个指令分层排列，图标补充 tooltip。样式只针对当前组件；teleport 内部通过当前抽屉专用类限定的 global 选择器生效，不修改共享 CSS。

既有 router 菜单改名、组件内无权限提示改名和 V200 迁移均保留。本任务没有后端、数据库、接口、权限或审计变更，没有提交或推送。

## 已执行

- `npm --prefix web run build`：最终退出 0，2562 个模块，28.22 秒；保留既有 PURE 注释及大包警告。首次沙箱执行被 esbuild 子进程权限拦截，批准本地执行后通过。
- `git diff --check`：退出 0。
- 只读脚本逐字对比所有业务处理函数与 HEAD：通过。
- 当前任务所有 JSON 解析：通过。
- `node scripts/check-all-governance.mjs`：退出 1。模块/Skill/开发入口检查通过，停止于历史 REQ-20260904-061 的 scope 文件不是 JSON-compatible YAML。
- 当前任务范围检查：退出 1，停止于现有 `licon` 分支命名不满足检查规则。实际新增修改面人工核对未超出本需求；不为通过门禁变更已有分支。

## 浏览器自测

本地管理员测试会话，路由 `/release-operations/drill-plans`。

- 桌面 1912px：目录 300px，详情 1280px；1280px：目录 300px，详情 633px。
- 手机 375x812、390x844、430x932：页面滚动宽度与视口一致；时序局部横滚，430px 下滚动到末列并打开编辑。
- 浅色桌面及三个手机视口截图、深色桌面页面与抽屉截图已检查。
- B 项目正向 4 时序/4 指令、回退 3/3；切换 A 项目后显示两条 A 项目方案，选择第二条后详情更新。
- A 项目正向时序指令数为 3、0、1、1、1，验证多指令和空时序呈现。
- 方案编辑、时序新增/编辑、指令新增弹层已打开检查；手机端指令抽屉合并时间范围和底部操作可达。
- 方案、时序、指令三级删除确认均执行取消，未出现失败提示且记录保留；没有实际删除、保存或写入测试数据。

浏览器命令偶发超时、视口缩放后出现点击延迟，均重新检查 DOM 后处理；临时视口覆盖已重置。

## 尚未验证

只读/无权限账号、网络失败及提交中故障注入、真实写入往返、完整网络/控制台记录、极端长文本、深色手机、独立人工复核。未将这些场景描述为通过。

任务前缀 `req-20260907-065-release-plan-page-redesign`，phase 为 `observing`。这是实现后的非独立自测，不宣称已收敛或完整验收。

## 上线与回退

上线检查该路由、两个时序抽屉、项目切换、只读权限和保存流程。回退仅撤销本需求的组件模板及局部样式 hunks，保留原菜单改名与 V200；无需数据补偿。
