---
id: REQ-20260907-068
status: ready
owner: rokeyvvz0828
module: business/release
---

# 投产演练示范数据

用户已确认添加测试数据，并明确“投产演练示范项目 A 和B，就用当前库”。当前库经本地配置核验为 ccb_platform_licon，项目ID分别940001、940002，tenant_id=1。

每个项目新增3轮（待演练、演练中、已完成），每轮6步，共6轮36步。使用项目各自现有方案100942001/100943001、环境200942001/200943001。所有内容虚构、标记批次REQ068；负责人不指定，不引入真实个人信息。保留现有全部数据。

仅在上述本地库的rel_release_drill_round与rel_release_drill_step插入。应用代码、权限、账号、项目主数据、数据库结构和已发布迁移不变；不重启或提交推送。使用事务、稳定批次ID、前置断言及幂等插入。created_by/updated_by=0表示离线测试夹具，不伪造真实用户操作；本任务记录作为数据装载证据，不冒充业务API审计。

验收：提交后新连接重读6轮36步，检查状态/时间顺序/关联；重复执行不新增；原有两表记录摘要不变。失败事务回滚，清理仅限未被后续编辑或引用的本批次ID。SQL验证不等同页面验收。

目标模块business/release，Owner rokeyvvz0828。已确认继续当前工作区，仅写本需求SQL/脚本与任务前缀记录；详细边界见codex-task-scope.yaml。
