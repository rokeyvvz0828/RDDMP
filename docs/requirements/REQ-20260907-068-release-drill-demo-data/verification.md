# 数据装载结果

已向本机当前库 ccb_platform_licon 写入REQ068批次：项目940001（投产演练示范项目 A）新增第5-7轮，项目940002（投产演练示范项目 B）新增第4-6轮。每轮6步，共6轮36步。当前活动轮次合计A7轮、B6轮。

覆盖已完成、演练中、待演练。步骤覆盖待开始、进行中、已完成、已跳过；使用虚构描述和结果，负责人未指定。复用本项目既有方案与环境，未创建其他业务数据。

## 证据

- node --check seed.mjs：退出0。
- seed.mjs --dry-run：退出0，模拟6轮36步后ROLLBACK，新增批次行数0。
- seed.mjs --apply：退出0，提交6轮36步。
- 再次--apply：退出0，新增0轮0步。
- seed.mjs --verify：新连接逐项验证项目、状态、轮次名称、步骤数、顺序、时间区间和关联，通过。
- 存量两项目的10条轮次记录（含已删除）和1条步骤记录SHA256前后一致。
- git diff --check：退出0，仅既有源码CRLF提示。

完整命令、计数、摘要和ID见本任务前缀的database-evidence.json、execution-T1.json；全部新增ID在fixtures.json。

## 范围与验收

本任务只新增当前需求的脚本、SQL、ID清单及工程记录，业务代码、数据库结构、账号、权限和现有数据未修改；未重启、提交或推送。文档中的mock.release账号登录返回401，没有修改凭据或权限。数据通过用户授权的本地数据库事务装载，created_by/updated_by=0明确表示离线夹具；不伪造业务API审计记录。

账本前缀req-20260907-068-release-drill-demo-data，phase=observing，SQL自检通过，独立复核和浏览器验收未执行。不将数据库读取等同页面可见性验证；在投产演练页面切换A/B并刷新，可查找名称以[REQ068]开头的轮次。本次没有修改产品源码，未执行Maven/Vue构建；未再次运行已知被历史REQ061 YAML和licon命名阻断的全仓治理检查。

## 定向回退

没有执行删除。需要撤回时，在本机库检查fixtures.json所列6个轮次和36个步骤仍为tenant1、原project_id、row_version=0、created_by=0，并核对与seed.sql的字段值一致；检查没有后续新增步骤或问题记录引用这些轮次。任何已编辑或新增引用必须停止清理，由用户决定保留。

条件满足后，在一个事务中按清单精确删除这36个步骤，再删除6个轮次；删除前后核对影响行数，异常ROLLBACK。不得按宽泛名称、全表或项目范围删除。原有轮次、步骤及关联方案/环境一律保留。
