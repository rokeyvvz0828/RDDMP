# 整体验证进展（2026-09-07）

本文件区分实现、自动化、运行和浏览器证据。正式 phase 以本需求 control-state.json 为准；尚不声明整体收敛或允许上线。

## 已实现

- 系统负责人隐含参与，显式参与名单独立维护，部署单元继承系统资格。
- 按系统/部署单元展开默认任务负责人和名单；创建前逐任务调整，创建后带原因及版本分派。新负责人自动参与，旧负责人保留，名单为快照。
- 任务执行实时校验有效项目/系统/任务资格，管理员不能代执行；详情、报告、时间、检查项、附件、通知、工单入口统一实体授权。
- 原计划详情看板原位替换：与我相关/管理全貌、四列状态、SQL可见性统计分页，不新增跨计划看板。
- 资源申请、实例、网络访问源系统、批量明细和任务工单入口接入参与权限；审批办理保留原职责。
- 项目资格撤销与业务写事务锁串行；只读查询不持资格锁，逾期扫描跳过退出的计划负责人。
- 本轮修正任务详情负责人姓名、非执行人员只读提示；重试加载恢复人员映射，分工提示和标签使用现有语义主题变量。

## 自动化证据

日志均位于本机 TEMP，不记录令牌、真实个人数据或附件。

| 检查 | 真实结果 | 限制 |
| --- | --- | --- |
| 最后后端补丁聚焦测试 | refinement-final8.log，12类99项，退出0 | 不是全仓回归 |
| 上一轮 mvn test | full-final2.log，129类675项，退出0 | 不包含最后平台锁、空阻塞、备用实例与通知补丁 |
| 最新 mvn test | full-final9.log，130类684项，退出0 | 失败0、错误0、跳过0；含最后全部后端补丁 |
| 后端 install -DskipTests | install-final8.log，退出0 | 只安装，不是测试 |
| 最终 npm --prefix web run build | web-final12.log，退出0；vue-tsc及Vite32.45秒 | 既有大chunk与vueuse注释告警 |
| scope --base 695e917 --working-tree | scope12.log，67文件通过 | 新增本轮控制产物后另复验 |
| 治理及Flyway | governance12.log / flyway12.log，失败 | REQ061 scope非JSON兼容格式，历史V84_1迁移命名；不改他需求或已发布迁移 |

前一次全仓类缺失失败与热编译/install输出重写竞态同时发生；已停止watcher，最新全仓期间未执行install。

## 真实浏览器验收

本地8080后端、5173 Vite；平台能力升级项目。公开文档本地演示数据，不涉及生产。

验收模板：REQ066系统参与验收模板，按物理子系统任务“系统部署参与验收”，检查项“确认部署就绪”。
验收计划：REQ066参与分工验收，SP1788753425059354，路由 /architecture/plans/1788753425059354。

1. 员工门户系统负责人为演示研发工程师，显式参与人为演示测试负责人；创建预览默认带出二人及研发负责人。
2. 创建前改负责人为测试，原研发保留，新负责人不可单独移除；创建成功，1任务、1检查项。
3. 创建后管理员填写移交原因改回研发，看板和任务详情显示研发姓名，测试保留参与。
4. 原详情页“敏捷看板”原位替换；管理员默认与我相关0，切全部后未开始1，其余0。
5. 非参与管理员任务详情没有启动、完成、重开检查项入口，有管理分工能力及显式只读提示。
6. 原空模板无展开任务时显示空提示，创建按钮禁用；没有制造空计划。
7. 管理员可看既有资源申请，但新建申请“物理子系统”候选为空，没有因管理员身份放大系统申请资格；未保存或提交申请。
8. 重新登录后初次计划加载失败，重试可恢复；本轮使重试同时恢复人员姓名映射。未据此宣称全应用登录初始化已修复。

### 视口与主题

- 1280×800：浅色/深色看板四列，未开始1卡片；浅色分工表单字段和操作区可达。
- 375×812、390×844、430×932：浅色/深色页面和看板曾实测；本轮补齐分工弹层三种手机宽度、两种主题截图观察，负责人、参与人、原因及保存/取消可达。
- 发现深色提示和人员标签浅底浅字，局部改用既有语义主题后复验可读；未修改平台全局样式。
- 430稳定布局DOM测量document.scrollWidth=innerWidth=430；375/390稳定截图无非预期整页横向溢出。截图为工具会话内观察，不虚构仓库截图路径。
- 管理员页面曾检查控制台error为空；不将此结果外推到未执行的普通角色路径。

## 验收缺口与外部故障

1. 按 docs/integration/mock-data-contract.md 公开说明登录 mock.engineer，页面返回“账号或密码错误，请重试”。没有猜测其他口令、重置用户或读取敏感凭据。普通参与人启动→检查完成→已完成、撤权旧页拒绝的真实浏览器矩阵未闭合；相关服务端自动化证据与浏览器证据分开报告。
2. 所有新建/编辑/复制/批量资源路径及所有表单移动端状态并未逐一真实点击；不能用管理者空候选代替这些完整场景。
3. 独立非作者上线权限/数据库专项复核未取得，用户Owner实施审批不等同于上线专项复核。
4. 全仓治理基线故障未解决，不绕过Required Checks、不批准合并或发布。

## 不适用边界与回退

独立CLB/DNS/CERT专项工单没有结构化系统/部署单元字段，不能猜名称授权；独立入口保留申请人/流程职责，从任务发起额外校验有效执行资格，创建关联同一事务。未来字段迁移须单独明确。

仅追加V159迁移，保留历史分工及审计；上线前识别失效历史负责人并移交未完任务及未解阻塞。不自动扩权回填。权限故障优先暂停受影响写入口并前向修复，不简单回滚恢复旧宽权限，不删除新增关系及审计数据。未推送、合并或发布。

## 最终结果补录（12:29）

- 最新全仓 mvn test 退出0，full-final9.log：130类684项，失败0、错误0、跳过0；2026-09-07 12:29:38 BUILD SUCCESS，总28:43。覆盖最后平台锁、空阻塞、备用实例及通知补丁。
- 最终前端 web-final12.log 对应本轮最后语义主题和映射修复，退出0。
- 实现及UI修复提交 f17ab5d；T1-T6执行产物已补录，正式phase经脚本从executing进入observing。独立自动化观测与浏览器矩阵缺口记录在observation-integration-final.json。
- 尚不进入verifying/converged；无产品失败证据不代表未采样场景通过。

## 2026-09-07 普通角色真实验收与入口纠偏

- 用户授权后通过本地UI建立REQ066_EXECUTOR角色和req066.executor合成账号，仅6项查看/本人资源权限，无管理或审批权限；已加入项目、系统及验收任务，保留原负责人。密码不进入仓库。
- admin/admin123本地登录成功；原mock账号登录问题不再是普通角色验收阻碍。
- 普通角色首次启动失败，定位10个Controller入口仍要求管理权限。修正为允许查看角色进入服务端实体/责任人资格检查，管理接口不放开；任务时间保留既有责任人限制。
- 真实方法安全代理：修正前31项10失败；修正后与参与权限聚焦共83项，0失败0错误0跳过，rddmp-066-rbac-green.log。安装成功、后端重启health=UP。
- 同一普通角色实测启动成功、看板已开始1；完成检查成功、完成人显示REQ066验收执行人，任务100%。
- 新发现：完成后抽屉和摘要更新，但看板卡片仍旧；点击页面刷新后正确移入已完成1。需继续修复，不能宣称交互验收全部通过。
- 本版本全仓回归正在执行，rddmp-066-full-rbac.log；前文684项是修正前证据。
- 看板同步已修正并真实复验：重新打开→已开始1，登记阻塞→阻塞1，解除→已开始1，再次完成→已完成1，全程未手动刷新，控制台error为空。web-board.log前端构建成功。
- 后续资源UI采样：普通申请人列表仅本人，但新建系统候选为空；代码发现选项Promise.all中的联系人/参数仍受物理系统查看权限拦截，403被静默吞掉。登记为后续纠偏，不通过给角色加架构管理权限掩盖。

## 2026-09-07 普通角色授权后的纠偏复验

- admin/admin123 本地真实登录成功；已按用户授权创建专用普通验收角色，仅含6项必要权限，不授管理/审批/超级管理员能力。
- F4：资源表单必要用户/参数选项支持资源申请权限，组织选项仍403；前端不再静默吞掉加载失败。方法安全代理测试同时覆盖无权限和不可访问项目。
- F5：撤销系统资格后，同一API会话读取计划详情/看板/时间视图均403、业务码40300，不再500。撤权期间旧资源草稿编辑/提交/复制及候选请求均拒绝；测试后恢复名单。API重放不冒充浏览器旧页验证。
- 最终聚焦：8类92项通过（rddmp-066-options-denial-green.log）；首次失败属于mock容器注入交互传感器误报，排除初始化交互后请求业务断言未放宽。
- 最终前端构建通过（rddmp-066-web-options.log），保留既有chunk体积告警。此前全仓131类715项通过只覆盖前版；包含F4/F5的全仓仍在运行，另行补录最终结果。
- 普通角色真实资源表单新建保存、编辑回填、手机保存通过。375×812、390×844、430×932及桌面分别观察明暗主题，内部滚动与底部操作区可达；手机整页无横向溢出。最新控制台error=[]。截图为工具会话证据，没有宣称落盘。
- 合成资源主单RR1788760806959026已拒绝，复制草稿1788760836273012及UI草稿RR1788761891729496均已取消；没有资源下发。浏览器恢复浅色及默认桌面视口。
- 本次scope检查88文件通过，diff --check通过。既有REQ061范围文件非JSON兼容YAML和V84_1迁移命名仍导致治理检查失败；不越范围修改、不绕过Required Checks。
- 尚未代签：非作者上线权限/数据库专项复核。跨项目真实部署单元及附件全部UI路径未逐项覆盖，不以单测/API覆盖冒充全量UI验收。当前phase为observing，不宣称converged或可上线。

### 2026-09-07 14:43 最新全仓最终结果（替代上述“仍运行”状态）

- `mvn test` 退出1，耗时27:28；实际执行121类688项，1失败、0错误、0跳过。ccb-test-management和ccb-boot模块因前序失败未运行，不得计入已通过。
- 唯一失败为 `ArchitectureOptionsControllerTest.eachKnownContext兼容旧读取权限并纳入新三级权限`：physicalUsers精确断言仍为旧权限表达式；physicalParameters也仍使用旧期望，首次断言失败后未继续执行。应仅同步这两条期望，保留organizations/businessComponents限制及真实方法安全代理测试，不删除、不跳过测试。
- 这是本次修复遗漏的关联测试同步，不归因于既有治理问题。日志：本地TEMP/rddmp-066-full-options-denial.log，22605起；对应结构化记录observation-full-regression.json。
- `server/src/modules/architecture/src/test/java/com/ccb/architecture/web/ArchitectureOptionsControllerTest.java` 未纳入当前scope精确可写列表；未擅自修改或扩写scope。T5记录为blocked，phase保持observing，等待该单文件范围确认后同步断言、重跑聚焦和全仓。

### 2026-09-07 15:24 最终回归通过（闭合旧权限断言遗漏）

- 用户明确同意单测试文件纳入scope，授权记录见implementation-admission.md；提交9f71fa7只同步physicalUsers和physicalParameters两条精确期望，未删除或跳过测试，组织和业务组件选项断言保持原限制。
- 聚焦测试3类19项通过，退出0，日志TEMP/rddmp-066-options-contract-green.log。
- 最新 `mvn test` 于2026-09-07 15:24:28结束，耗时37:09，退出0。131类717项，失败0、错误0、跳过0，包含ccb-test-management与ccb-boot，全部模块SUCCESS。日志TEMP/rddmp-066-full-contract-final.log。
- 上述结果闭合前次全仓失败及测试文件范围缺口；前次失败记录保留用于追踪。本轮仅测试和文档变更，上一轮生产构建与本地UI/API证据仍适用，但未冒称本轮重测浏览器。
- 仍不宣称converged或已准予上线：既有治理/Flyway命名门禁、非作者权限/数据库专项复核及已披露未逐项执行的跨项目部署单元/附件UI路径仍须分别处理。当前控制phase为observing。
- 回退本轮只需回退测试提交及对应范围文档，不涉及数据或生产行为；业务权限修复不应为迁就旧断言而撤销。

### 2026-09-07 用户截图反馈：物理子系统操作按钮被裁切

- 用户反馈属实：新增参与人员入口后桌面操作列仍为160px，三个操作不换行内容超出；本地管理员复现内容可用宽135px、scrollWidth200px，末按钮越过单元格约52px。此前验收未覆盖到这一处，不应将其他页面通过当作本页无缺陷。
- F-0006仅修改PhysicalSubsystemPage.vue操作列160→260，保留固定右列、UiDataTable、原按钮权限及手机卡片；未引入新样式、业务写入、迁移或公共组件变更。
- 1440桌面及原851窄桌面修复后均235/235无内容溢出，全部按钮在单元格内，刷新仍生效；发起变更菜单及参与人员弹层可打开，未发起工单也未保存名单。
- 375×812、390×844、430×932和1440×900明暗主题均观察通过，手机仍使用卡片，操作区完整；控制台error为空，结束恢复浅色及默认视口。
- 本轮前端构建退出0，日志TEMP/rddmp-066-physical-actions-build.log，保留既有chunk告警；后端未变，本轮未重跑717项后端测试。
- 回退：回退本次单行列宽提交，不影响数据。全局验收缺口保持原记录，phase仍为observing。

## 2026-09-07 参与人员抽屉整理与原因选填

- 已按用户确认保留抽屉，负责人独立卡片、全量人员标签换行、原因选填、成功关闭刷新；不改账号名、资格规则、公共组件或表结构。设计见 `participants-drawer-refinement.md`。
- 后端先红：空/null/空白原因3项被旧实现拒绝；修正后服务18项、Controller6项、MySQL6项，共30项全部通过，0跳过。日志：`TEMP/rddmp-066-drawer-green.log`。
- 最终前端构建成功（26.65s），既有大chunk告警。`mvn -pl :ccb-boot -am install -DskipTests`成功仅作为本地运行构建证据，非测试通过证据；本地后端已重启至新版本。
- 本地admin不填原因、名单不变保存成功关闭，重开仍3人；数据库最新审计原因长度0，操作人/时间存在，变更前后名单一致。没有伪造原因。
- 1440×900及375×812/390×844/430×932明暗主题截图检查；手机成员标签换行、底部按钮可达；桌面抽屉640/640无横向溢出；搜索、无匹配、未保存保护已实测；控制台error=[]。截图为浏览器工具会话内证据。
- 全仓治理仍受REQ061 YAML格式阻塞；Flyway静态检查仍受既有V84_1命名阻塞，未越界改动。
- 本次未重跑全仓717项，也未重新实测只读角色/网络失败/超长名单；不将此前证据冒充本轮结果。上线非作者专项复核与原整体缺口保留，账本保持observing，不宣称整体converged。
- 回退：回退本次有界提交，保留既有空原因审计；无数据库回退。

## 2026-09-07 F-0007 遮罩关闭守卫纠正

用户报告：编辑后点击遮罩触发确认，继续编辑后抽屉消失且无法重开。1440×900本地admin浏览器成功复现，确认框出现时底层抽屉已隐藏，继续编辑后再点击参与人员仍无dialog。上一轮仅测底部取消，漏测内部关闭入口。

原因：UiFormDrawer透传的update:modelValue来自抽屉内部关闭之后；业务层拒绝更新父model不能阻止内部visible已经变false，形成父子状态不一致。

限幅修复：只修改SubsystemParticipants.vue，增加透传before-close前置守卫，以受控model关闭；复用在途确认Promise；确认框closeOnClickModal/closeOnPressEscape设false；过期确认不操作新上下文。不修改公共抽屉、后端、权限、审计或数据库。

复验：
- 原遮罩路径现在确认时抽屉仍在；继续编辑保留输入；放弃关闭后可重开。
- 右上角关闭、抽屉Esc、确认框右上角关闭、底部取消均实测；确认框Esc不退出。
- 375×812手机确认框、继续编辑、放弃、重开和无修改关闭实测；1440×900桌面流程通过；控制台error=[]。本轮未重复完整四视口明暗矩阵，无布局样式改动。
- 原失败路径使用真实遮罩点击；确认框遮罩禁止关闭由实际配置及5项自动化探针断言验证，没有声称坐标点击确认框遮罩的独立实测。
- 临时Node测试抽取实际关闭函数，5项全部通过，覆盖确认去重、取消、过期、提交中、干净关闭和before-close不提前调用done；脚本/日志在TEMP/rddmp-066-drawer-close-regression.cjs及.log，非完整Vue挂载测试。
- npm --prefix web run build通过（27.30s），既有chunk告警；本轮不涉及后端，未重跑后端测试。

未保存测试输入均已放弃，未改业务名单。可回退本次独立提交；此前整体上线复核/治理缺口保留，账本不宣称整体收敛。


## 2026-09-07 零任务创建与公共任务姓名/分派修复

- 零任务成功预览可创建计划，状态未开始；空进度不伪装100%；有任务缺负责人仍阻止提交，陈旧空预览重新校验。部分目标未配置专属任务明确提示，公共任务不强行复制。
- 公共任务候选使用有效项目成员与有效用户交集的用户姓名；默认负责人不具备成员资格时留空，允许指定合法人员。系统和部署单元继续继承系统分工。
- 修复真实HTTP发现的公共任务预览/生成key类型不一致，避免修改负责人后仍使用默认值。增加对应创建服务回归测试。
- 后补任务增加负责人/参与人表单和只读默认分工接口，要求管理及实体权限、同计划有效目标，越界返回403；无有效负责人时禁用保存，可改选；自动参与且旧负责人默认保留。
- 最新聚焦测试82项全部通过，0跳过（37.676s）；前端生产构建通过（27.05s），既有chunk告警；install -DskipTests通过仅表示本地运行构建成功，后端已重启至最终版本。日志见TEMP/rddmp-066-plan-creation-tests-final.log及-build-final.log。
- 真实浏览器/HTTP：姓名预览并创建；手机零任务创建后补公共及系统任务；修改系统任务负责人成功；无成员资格的计划负责人创建预览为空、改选保存成功，后补界面改选成功；最终空计划1788774666898131保持未开始、null进度、1环节0任务，敏捷看板四列为0。
- 1440×900、375×812、390×844、430×932浅色及1440×900、375×812深色表单检查，无横向溢出、操作可达；未重复390/430深色及网络失败注入。新验收页面console error=[]，旧热更新会话出现过LoginView初始化错误，未当作无错误通过。
- 直达详情首次加载曾显示操作失败，点击重试恢复，此现象本轮未定位，不混同为本轮两个原始问题已覆盖。全仓治理受既有REQ061 YAML格式阻塞，未越界修改；本轮未跑全仓测试/Flyway检查。上线前非作者专项复核仍保留。
- 全部修改限定本需求scope，无数据库迁移；回退本轮提交即可回退代码，合成本地回归计划保留，不删除用户数据。账本prefix=req-20260906-066-subsystem-participation，phase保持observing，不宣称整体收敛。
