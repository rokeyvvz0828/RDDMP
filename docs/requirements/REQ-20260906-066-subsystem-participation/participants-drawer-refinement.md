# 参与人员抽屉整理（2026-09-07）

## 来源和范围
用户确认保留抽屉、变更原因选填，并以“可以”批准负责人独立展示、名单不折叠、保存成功关闭并刷新方案。这是需求调整，不作为缺陷或新增审批记录。延续当前T1；不更改账号姓名、资格规则、任务分工或公共组件。

## 设计与实施计划
1. `SubsystemParticipationServiceTest.java`新增null、空白、正常原因及超过500字符测试，断言名单审计与操作审计；先红后绿。
2. `SubsystemParticipationService.java`允许null/空原因，统一为空字符串入审计，保留500字符上限、版本/名单/权限/移交检查。不变更SQL、历史迁移、DTO和表结构。
3. `SubsystemParticipants.vue`复用示范中心的UiFormDrawer、顶部标签表单和语义色；负责人独立头像/姓名/默认参与标签；其他成员全量换行且可搜索移除；原因选填；成功短消息、关闭、刷新。只读、加载、失败、提交中和未保存保护不变。
4. 当前示范表单没有“不可编辑负责人+显式成员”组合，因此只新增本组件scoped样式，用既有语义变量；内容纵向滚动、名单局部滚动，底部操作固定；不新增公共样式。桌面640px上限，手机不超视口。
5. 执行聚焦Maven测试、前端构建、范围/空白检查；浏览器检查桌面与375×812、390×844、430×932明暗主题、未保存提醒、空原因保存关闭及重开。

## 验收与回退
空原因不阻止合法保存，500字符上限不放宽；不伪造原因。负责人不重复显示为其他成员；全量已选姓名可通过换行/内部滚动读取。保存失败保留输入，成功关闭。保留服务端授权与审计。
按本次独立提交回退代码即可，空原因历史审计保留。测试未执行则不得称通过；上线非作者权限/数据库复核仍为原需求待办。
