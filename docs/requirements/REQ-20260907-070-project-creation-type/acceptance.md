# 项目创建类型执行记录

REQ-20260907-070：2026-09-08已部署到当前开发库及后端。任务按control-engineering和rddmp-delivery-engineer的证据要求保留observing，不声明浏览器或完整回归验收通过。

## 2026-09-08 部署结果

- 最终备份：`.runtime-logs/req070-preserve-data/before-20260908-090849.sql`，保留此前备份；未提交备份、日志或配置凭据。
- 在独立MySQL中恢复最终备份，执行两条兼容迁移；真实Flyway校验通过。
- 当前库实际执行V199与本地兼容V200，未重置、repair或伪造历史成功记录。原迁移历史的逐列哈希保持一致。
- 250张原有业务表逐列哈希一致；12张投产表的字段、索引和关联约束一致。保留旧表、旧列及权限记录，新增15张缺失的数据迁移表并补齐必要字段/索引。
- 兼容V200是独立的本地迁移，不等于执行规范目录V157-V198；没有把这些被排除的迁移标记成功。规范目录仍有V156冲突，需要另行统一，不能直接使用默认classpath启动替代本次兼容启动。
- V35-V46的旧文件名是历史记录遗留，实际校验和已匹配当前脚本；不应仅凭旧名称重跑迁移。
- 隔离库类型改为CONTINUATION后提交，新连接读取通过，随后恢复类型和更新时间。旧探针曾遗漏恢复更新时间，已修正并用全新副本复验。
- 后端Java17启动，8080健康检查UP；前端5173返回200；匿名项目接口返回401。启动关闭Mock同步及演示工作流发布以避免改写数据。
- 本轮后端模块构建/安装完成；此前ProjectServiceTest 42个用例及前端类型同步专项检查通过。旧JAR占用导致打包失败后改用已构建模块的classpath启动。
- 临时18070后端及两个任务测试MySQL容器、匿名卷已清理。当前后端8080与原前端5173保留运行。
- 本次为同一上下文验证；尚未执行真实浏览器登录、编辑保存与刷新验收。

启动、验证命令与兼容范围见 `deployment.md`。

## 实际改动

- ProjectService.java：创建/更新仅允许NEW和CONTINUATION；创建缺省NEW、更新缺省保留；两个项目读取SELECT返回creation_type；原权限和audit不变。
- ProjectServiceTest.java：增加12个用例，覆盖枚举、缺省、空值/非法值、SQL参数、读取投影和审计调用。
- V199__project_creation_type.sql：追加列、默认值及CHECK；未修改历史迁移。
- web/src/types/project.ts和project-context.ts：统一类型、中文标签和顶部元数据。
- web/src/stores/project-context.ts：复用保存响应同步元数据，防止在途旧列表覆盖新值，不调用select或发起请求。
- web/src/views/ProjectView.vue：创建/编辑类型选择、校验和保存后同步；仅该弹窗的响应式宽高与正文滚动。
- web/src/views/AppLayout.vue：三个顶部选择器当前值和下拉选项标签；原确认逻辑保持。
- 本需求的creation-type.test.mjs及控制记录。

以上产品改动都在批准的精确范围内。未覆盖其他投产、路由或并行改动；没有修改公共UiStatusTag、API路径、权限模型、依赖或生产系统。

## 实际验证

- 开发入口插件检查通过。
- 默认Java25导致Mockito初始化失败；仅为本次测试进程设置JAVA_HOME到本机JBR17，原30测试通过，未修改全局环境。
- 测试先行：42个测试中11个在实现前因缺少字段持久化/校验失败；实现后42个全部通过。
- Java17下mvn -q -pl :ccb-system -am test退出0。
- Node前端检查通过：类型标签、旧响应默认值、即时同步、在途旧列表、加载失败、选择保持和三个label/option入口。测试编译目标已与应用现代JS运行环境对齐。
- 前端vue-tsc/Vite构建通过；保留既有PURE注释及大包警告。
- git diff --check退出0，仅换行规范提示。
- 隔离MySQL8.4验证V199通过：两条旧数据均NEW，新行默认NEW，续建可写，INVALID报ERROR3819。临时容器无网络/端口映射，已删除容器及其临时数据卷。
- 全量mvn test首次因沙箱Docker限制失败；授权重跑后发现既有两个V156迁移冲突，workflow集成测试失败。未修改这些既有文件，完整回归未通过。
- check-all-governance仍被历史REQ061范围YAML格式阻断。
- 本任务scope必需元数据已补齐，重跑范围检查仍被现有licon分支命名阻断；未为通过检查改分支或扩展产品范围。

## 未决与上线顺序

真实浏览器四视口、明暗主题、三个导航布局和保存后网络/界面行为尚未验收，既有浏览器访问授权缺口未绕过。同一上下文检查不是独立验证。

共享环境上线前仍须统一V156版本冲突并重新验证规范迁移链。本地开发已使用独立兼容目录完成V199/V200和后端部署，可以开始业务验收；本地兼容方案不能直接当成生产发布方案。

创建类型不改变项目编码作为切换引用的既有模型；本次未扩展项目重命名/编码迁移规则。

## 回退

仅反向撤销本次字段、标签、同步和专项样式增量。已执行的迁移保留新增列和续建数据，不删除列、不改历史SQL；后续数据库修复走追加迁移及人工复核。未提交或推送代码。
