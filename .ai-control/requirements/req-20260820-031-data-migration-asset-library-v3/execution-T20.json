{
  "task_id": "T20",
  "status": "implemented",
  "changed_surfaces": [
    {"path_or_resource": "web/src/api/data-migration.ts", "purpose": "Require complete issue update payloads with all three relation arrays"},
    {"path_or_resource": "web/src/modules/data-migration/views/content/IssuesPage.vue", "purpose": "Load complete issue detail and options before opening edit and submit explicit relation arrays"}
  ],
  "requirements_addressed": ["R5", "R8"],
  "commands": [
    {
      "command": "npm --prefix web run build",
      "exit_code": 0,
      "salient_output": "vue-tsc --noEmit and Vite build succeeded; 2450 modules transformed; built in 12.00s"
    },
    {
      "command": "git diff --check",
      "exit_code": 0,
      "salient_output": "No whitespace errors"
    }
  ],
  "local_checks": [
    {
      "check": "Detail-gated edit state",
      "result": "pass",
      "evidence": "openEdit closes prior state, awaits getIssue, project options and selected-table field options, then sets editing/editId and opens the drawer; catch resets form and leaves drawer closed",
      "limitations": ["Network-failure behavior will be exercised in the T21 browser run"]
    },
    {
      "check": "Explicit relation serialization",
      "result": "pass",
      "evidence": "IssueUpdateData requires relatedMeetingMinutes, relatedTables and relatedFields; doSave always supplies cloned arrays including empty arrays",
      "limitations": ["Network request payload inspection remains for T21"]
    },
    {
      "check": "Frontend compilation",
      "result": "pass",
      "evidence": "Vue TypeScript checking and production bundle generation completed with exit code 0",
      "limitations": ["Build reports existing large-chunk warnings"]
    }
  ],
  "diff_summary": [
    "Imported and awaited getIssue before establishing editable state",
    "Loaded relation option labels before opening the existing responsive drawer",
    "Made issue update request type require all three relation arrays",
    "Serialized cleared relations as empty arrays and prevented duplicate edit loads with busy state"
  ],
  "invariants_checked": [
    {"invariant": "List endpoint remains lightweight", "result": "pass", "evidence": "listIssues and list rendering are unchanged; only the edit action calls getIssue"},
    {"invariant": "Failed detail load cannot expose a submittable drawer", "result": "pass", "evidence": "drawer is set false before requests and set true only at the end of the successful try block"},
    {"invariant": "Existing UI and responsive layout are reused", "result": "pass", "evidence": "UiPageHeader, UiToolbar, UiDataTable and UiFormDrawer composition and drawer width are unchanged"}
  ],
  "disturbances": [
    {"event": "Vite reports an existing main chunk larger than 500 kB", "impact": "Build succeeds; bundle optimization is outside the bounded issue-governance task"},
    {"event": "Rollup removes two unrecognized PURE annotations from @vueuse/core", "impact": "Third-party warning only; emitted bundle succeeds"}
  ],
  "assumptions_falsified": [],
  "unresolved_items": ["Browser failure injection, network payload inspection and four-viewport overflow checks remain for T21"],
  "recommended_next": "observe"
}
