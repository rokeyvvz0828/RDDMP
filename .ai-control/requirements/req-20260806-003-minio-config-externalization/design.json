{
  "schema_version": 1,
  "topic": "req-20260806-003-minio-config-externalization",
  "objective": "remove environment-specific MinIO defaults from Java and fail fast on invalid external configuration",
  "requirements": [
    {"id": "R1", "statement": "Java properties contain no endpoint, bucket or expiry defaults", "priority": "must"},
    {"id": "R2", "statement": "required values and expiry bounds are validated", "priority": "must"},
    {"id": "R3", "statement": "storage behavior and external configuration keys remain compatible", "priority": "must"}
  ],
  "configuration_source": "Spring application configuration and environment placeholders",
  "security": "credentials remain mandatory external values and are not emitted in tests or logs"
}
