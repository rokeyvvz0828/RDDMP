{
  "schema_version": 1,
  "topic": "platform-business-integration",
  "design_revision": 1,
  "design_status": "approved",
  "human_document": "docs/engineering-control/designs/2026-08-14-platform-business-integration-design.md",
  "objective": "Provide reusable workflow business integration, durable lifecycle delivery, dashboard task context, persistent attachments, internal session-confirmed signatures, and a replaceable Mock project context without moving business state into platform modules.",
  "users": [
    "Business-module developer integrating workflow and attachments",
    "Approver using dashboard tasks and internal electronic signatures",
    "Business user accessing authorized attachments",
    "Platform administrator observing events, signatures, and attachment cleanup"
  ],
  "requirements": [
    {
      "id": "R1",
      "statement": "A server-side business module must be able to start the current valid published workflow version by tenant-scoped stable definition code.",
      "acceptance": ["Unknown, unpublished, or invalid codes create no instance", "A successful instance fixes definition ID and version", "The legacy definition-ID start remains compatible"],
      "priority": "must",
      "source": "User approved fixed-code workflow startup after latest main inspection",
      "counterexample": ["The business module queries wf_definition", "The browser chooses a definition ID"]
    },
    {
      "id": "R2",
      "statement": "Workflow instances must persist consistent business type, key, title, round, project display snapshot, action path, and data digest.",
      "acceptance": ["Inbox, done, detail, timeline, and lifecycle events expose the same context", "Action paths accept only internal routes"],
      "priority": "must",
      "source": "User approved business context for dashboard and release detail navigation",
      "counterexample": ["A dashboard task shows only businessKey", "An external URL is stored as actionPath"]
    },
    {
      "id": "R3",
      "statement": "Workflow state changes and immutable business lifecycle events must be stored in one transaction.",
      "acceptance": ["Committed workflow transitions retain an event across process restart", "Rolled-back transitions leave no event"],
      "priority": "must",
      "source": "User approved durable lifecycle events",
      "counterexample": ["An in-memory callback is the only record", "The event commits before a failed workflow transition"]
    },
    {
      "id": "R4",
      "statement": "Lifecycle events must be delivered at least once per stable subscriber with persistence, bounded retry, DEAD visibility, and consumer idempotency.",
      "acceptance": ["A temporarily failing consumer later succeeds", "Duplicate delivery has one business effect", "An authorized retry can recover DEAD delivery"],
      "priority": "must",
      "source": "User approved idempotent pass, return, and terminate events",
      "counterexample": ["A consumer outage silently drops approval", "Retry generates a new event identity"]
    },
    {
      "id": "R5",
      "statement": "Legacy and Flowable workflow paths must emit the same STARTED, RETURNED, APPROVED, REJECTED, and TERMINATED event contract.",
      "acceptance": ["Contract tests cover both engines", "Business consumers do not branch on engine type"],
      "priority": "must",
      "source": "Current repository has two workflow execution paths",
      "counterexample": ["Only Flowable completion creates an event", "Payload semantics differ by engine"]
    },
    {
      "id": "R6",
      "statement": "The dashboard must show up to five current-user inbox tasks and five recent done tasks with links to full lists.",
      "acceptance": ["Loading, empty, error, forbidden, and populated states render", "Full workflow inbox and done pages remain available"],
      "priority": "must",
      "source": "User requested unified task information on the home workbench",
      "counterexample": ["The dashboard uses hardcoded task cards", "The full workflow pages are removed"]
    },
    {
      "id": "R7",
      "statement": "Workbench tasks must show business title, type, project display name, current node, actor, time, result, and an authorized internal business-detail route.",
      "acceptance": ["A task opens the intended business detail", "Missing legacy metadata has safe fallback display"],
      "priority": "must",
      "source": "User approved enriched dashboard task context",
      "counterexample": ["The UI constructs a route from an unvalidated external URL", "A task is identified only by a numeric instance ID"]
    },
    {
      "id": "R8",
      "statement": "Workflow approval nodes must support a backward-compatible signatureRequired setting that blocks decision until explicit confirmation.",
      "acceptance": ["Existing nodes default to unsigned behavior", "A required node rejects an unconfirmed decision", "The designer exposes the setting"],
      "priority": "must",
      "source": "User approved workflow-configured internal electronic signatures",
      "counterexample": ["Every old workflow suddenly requires a signature", "The requirement exists only in frontend state"]
    },
    {
      "id": "R9",
      "statement": "Internal signatures must use the current authenticated identity, bind task, round, action, comment, and business digest, and remain immutable.",
      "acceptance": ["The server ignores client signer identity and does not accept a client-supplied digest", "Approval and signature commit atomically against the stored instance digest", "Timeline and detail expose the signature evidence"],
      "priority": "must",
      "source": "User selected current-login click confirmation without password re-entry",
      "counterexample": ["The client can submit another signer ID or replace the signed digest", "A signature is editable after approval"]
    },
    {
      "id": "R10",
      "statement": "A new ccb-attachment platform module must own persistent attachment metadata, storage identity, state, binding, and audit.",
      "acceptance": ["Business modules store only attachment IDs", "Object keys remain private", "The attachment module has an independent Maven boundary"],
      "priority": "must",
      "source": "User approved persistent business attachments",
      "counterexample": ["The release module writes MinIO object keys", "Temporary preview records are treated as permanent attachments"]
    },
    {
      "id": "R11",
      "statement": "TEMP attachments must be restricted to their uploader while BOUND attachments must use a fail-closed business AttachmentAccessPolicy.",
      "acceptance": ["Changing an attachment ID cannot bypass tenant or owner checks", "A missing or failed policy denies access", "Repeated identical binding is idempotent"],
      "priority": "must",
      "source": "User approved tenant, project, and business ownership controls while formal project authorization remains deferred",
      "counterexample": ["Any authenticated user can delete a known attachment ID", "Policy failure grants access"]
    },
    {
      "id": "R12",
      "statement": "Authorized attachments must support metadata, preview, download, controlled deletion, immutable binding evidence, and retryable orphan cleanup.",
      "acceptance": ["kkFileView preview is reused when supported", "Unsupported files can still download when authorized", "Expired unbound files are cleaned idempotently"],
      "priority": "must",
      "source": "User approved the complete attachment lifecycle",
      "counterexample": ["Bound attachments are deleted by the generic browser endpoint", "Cleanup deletes an active bound attachment"]
    },
    {
      "id": "R13",
      "statement": "The frontend must expose a replaceable ProjectContextProvider and Store with temporary Mock projects and persisted current selection.",
      "acceptance": ["AppLayout, dashboard, and future business pages consume the Store", "Replacing the provider does not change consumer APIs"],
      "priority": "must",
      "source": "User stated the project module is in progress and requested a page Mock first",
      "counterexample": ["Every page embeds its own project array", "Project selection disappears on refresh"]
    },
    {
      "id": "R14",
      "statement": "Mock project context and Mock project membership must be presentation and local-integration data only, never a production server authorization source.",
      "acceptance": ["Manipulating browser storage does not grant server data access", "Formal project authorization remains an explicit dependency"],
      "priority": "must",
      "source": "User deferred project membership and data-scope implementation",
      "counterexample": ["A client projectRef is trusted as permission", "Production is declared project-isolated using only Mock data"]
    },
    {
      "id": "R15",
      "statement": "Existing workflow by-ID startup, designer, inbox, done, notification publisher, and temporary file-preview behavior must remain compatible.",
      "acceptance": ["Existing tests and user flows pass", "New fields are nullable for old workflow instances", "signatureRequired defaults false"],
      "priority": "must",
      "source": "Repository compatibility contract and selected incremental approach",
      "counterexample": ["The existing workflow API is removed", "The notification center is reimplemented"]
    },
    {
      "id": "R16",
      "statement": "Business modules must use public workflow and attachment contracts without platform dependencies on concrete business modules or private table access.",
      "acceptance": ["Platform modules compile without ccb-release", "Business integration passes public-contract tests", "No business repository writes wf or att tables"],
      "priority": "must",
      "source": "Platform AGENTS and user requirement for reusable platform capabilities",
      "counterexample": ["ccb-workflow imports com.ccb.release", "A release service updates wf_instance directly"]
    }
  ],
  "invariants": [
    "Workflow terminal state and emitted terminal event remain consistent",
    "Signer identity comes only from the authenticated server principal",
    "Signature and bound attachment evidence is not physically deleted",
    "Object storage keys are never exposed to business clients",
    "Platform modules never depend on concrete business modules",
    "Mock project data never grants server authorization"
  ],
  "constraints": [
    "Java 17, Spring Boot 3.4.4, MySQL 8.4, Vue 3 and TypeScript",
    "Append-only Flyway V35 and V36; V1 through V34 remain unchanged",
    "Preserve existing com.ccb packages, APIs, and old workflow behavior",
    "Reuse SystemNotificationPublisher and kkFileView capabilities",
    "Public platform changes require full governance and regression verification",
    "Desktop workbench and approval acceptance only for this requirement"
  ],
  "non_goals": [
    "Formal project management and project-member authorization",
    "CA or legally binding digital signature",
    "Password, SMS, biometric, email, or external signature verification",
    "External event bus or cross-system integrations",
    "Release application, baseline, and report business logic",
    "Artifact repository and artifact file upload"
  ],
  "selected_approach": {
    "name": "Incremental workflow public contracts plus independent attachment module and replaceable frontend project provider",
    "reason": "It preserves current platform behavior, keeps business modules independent, and creates reusable capabilities without building a general integration platform.",
    "alternatives": [
      {"name": "Implement adapters and attachments inside ccb-release", "rejected_because": "It duplicates platform concerns and cannot be reused by later business modules."},
      {"name": "Build a generic cross-system event and low-code integration center", "rejected_because": "It adds protocols and operations not required by the current modular monolith."}
    ]
  },
  "architecture": {
    "boundary": {
      "inside": ["workflow code gateway", "workflow business context", "durable lifecycle delivery", "dashboard task projection", "internal signature", "persistent attachment center", "Mock project provider"],
      "outside": ["formal project backend", "business state machines", "external signatures", "external messaging", "artifact storage"]
    },
    "components": [
      {"name": "workflow-business-gateway", "responsibility": "Resolve published definitions by code and start, terminate, or query business workflows", "depends_on": ["existing workflow services"]},
      {"name": "workflow-lifecycle-outbox", "responsibility": "Persist, dispatch, retry, and expose lifecycle event deliveries", "depends_on": ["workflow state transitions"]},
      {"name": "workflow-signature", "responsibility": "Validate signature-required decisions and store immutable session-confirmed evidence", "depends_on": ["authenticated user", "workflow task", "business digest"]},
      {"name": "workflow-task-projection", "responsibility": "Return enriched inbox, done, detail, timeline, and signature information", "depends_on": ["workflow business context"]},
      {"name": "attachment-center", "responsibility": "Own temporary and bound file lifecycle, policies, preview, download, deletion, audit, and cleanup", "depends_on": ["MinIO storage", "file-preview public provider", "authenticated user"]},
      {"name": "dashboard-workbench", "responsibility": "Render compact current-user inbox and done summaries with business navigation", "depends_on": ["workflow task API", "project-context Store"]},
      {"name": "mock-project-provider", "responsibility": "Provide replaceable local project display context", "depends_on": ["browser local storage"]}
    ],
    "interfaces": [
      {"name": "WorkflowBusinessGateway", "producer": "ccb-workflow", "consumers": ["business modules"], "contract": "Resolve and start a published workflow by stable code, terminate a validated business instance, and query progress."},
      {"name": "WorkflowLifecycleConsumer", "producer": "business modules", "consumers": ["workflow lifecycle dispatcher"], "contract": "Declare subscriber key and supported business types, then consume stable at-least-once events idempotently."},
      {"name": "AttachmentGateway", "producer": "ccb-attachment", "consumers": ["business modules"], "contract": "Bind, inspect, and perform business-authorized attachment lifecycle operations using opaque IDs."},
      {"name": "AttachmentAccessPolicy", "producer": "business modules", "consumers": ["ccb-attachment"], "contract": "Authorize a current user for a business key and operation without exposing storage identity."},
      {"name": "ProjectContextProvider", "producer": "frontend application", "consumers": ["AppLayout", "dashboard", "business pages"], "contract": "Expose current and available projects behind a replaceable provider."}
    ],
    "data_flows": [
      {"input": "Stable workflow code and trusted business context", "transitions": ["resolve current published version", "validate model and route", "start fixed version", "persist STARTED event"], "output": "Workflow instance and durable event"},
      {"input": "Workflow decision or termination", "transitions": ["validate task and optional signature", "commit workflow state and signature", "persist lifecycle event", "dispatch with retry"], "output": "Consistent workflow evidence and eventual business notification"},
      {"input": "Authenticated multipart upload", "transitions": ["validate file", "store opaque object", "create TEMP metadata", "bind after business save", "authorize each access"], "output": "Persistent authorized attachment ID"}
    ]
  },
  "error_handling": [
    {"condition": "Workflow code missing, unpublished, or invalid", "behavior": "Create no instance and return a clear conflict.", "recovery": "Publish a valid definition and retry."},
    {"condition": "Lifecycle consumer fails", "behavior": "Keep committed workflow state and persist retry delivery.", "recovery": "Automatic backoff or authorized DEAD retry."},
    {"condition": "Signature confirmation or evidence write fails", "behavior": "Reject or roll back the workflow decision.", "recovery": "Restore the condition and resubmit while the task remains pending."},
    {"condition": "Attachment access policy is missing or fails", "behavior": "Deny bound attachment access.", "recovery": "Register or restore the business policy."},
    {"condition": "Preview service is unavailable", "behavior": "Keep attachment metadata and authorized download available.", "recovery": "Restore kkFileView and retry preview."},
    {"condition": "Orphan cleanup or object deletion fails", "behavior": "Keep a retryable cleanup job and hide logically deleted content.", "recovery": "Automatic or authorized manual retry."}
  ],
  "quality_attributes": {
    "security": ["server-derived identity and tenant", "fail-closed attachment policies", "validated internal routes", "private object keys", "immutable signatures"],
    "performance": ["five-row dashboard limits", "server-side task paging", "indexed event scheduling", "bounded retry and cleanup batches", "short-lived presigned URLs"],
    "compatibility": ["nullable context for old instances", "legacy by-ID API retained", "signatureRequired defaults false", "existing preview and notification APIs retained"],
    "operations": ["event backlog and DEAD visibility", "attachment cleanup retry", "signature and attachment audit", "feature-disable rollback without data deletion"]
  },
  "verification_strategy": [
    {"requirement_ids": ["R1", "R2", "R15", "R16"], "signal": "Stable-code gateway and old API both work without private table access.", "method": "Workflow unit, integration, compatibility, and architecture tests."},
    {"requirement_ids": ["R3", "R4", "R5"], "signal": "Both engines create durable events and retries converge without duplicate business effects.", "method": "Transactional, crash-window, duplicate, ordering, and retry tests."},
    {"requirement_ids": ["R6", "R7"], "signal": "Dashboard task data matches inbox and done APIs and navigates to internal business routes.", "method": "Frontend build and desktop browser acceptance."},
    {"requirement_ids": ["R8", "R9"], "signal": "Required signatures are atomic, server-attributed, immutable, and visible.", "method": "Workflow decision, authorization, persistence, and UI tests."},
    {"requirement_ids": ["R10", "R11", "R12"], "signal": "Attachment lifecycle preserves ownership, policies, evidence, preview, download, deletion, and cleanup.", "method": "Attachment module unit, integration, MinIO-adapter, and permission tests."},
    {"requirement_ids": ["R13", "R14"], "signal": "Mock project selection persists and cannot grant server authorization.", "method": "Store tests, browser tests, and tampered-client permission tests."}
  ],
  "assumptions": [
    {"text": "Business modules provide stable business context and normalized SHA-256 digests.", "basis": "Release design already requires immutable workflow rounds.", "falsifier": "A business module cannot produce a stable key or digest."},
    {"text": "Business consumers and attachment policies can be discovered as Spring beans in the modular monolith.", "basis": "Current Boot module composes all Maven modules.", "falsifier": "Deployment separates platform and business modules into different processes."},
    {"text": "The future project module can replace the frontend provider without changing consumer APIs.", "basis": "User stated project management is being implemented separately.", "falsifier": "The project module requires a fundamentally different multi-project interaction model."}
  ],
  "unknowns": [
    {"question": "What are the final production limits and retention duration for attachments?", "impact": "Configuration defaults only; lifecycle and data model are unchanged.", "owner": "Platform owner", "blocking": false},
    {"question": "What retry count and backoff values should production use for lifecycle delivery and cleanup?", "impact": "Operational tuning only; delivery semantics are unchanged.", "owner": "Platform operations", "blocking": false},
    {"question": "What DTO will the formal project service expose?", "impact": "Remote provider mapping only; Mock provider contract remains stable.", "owner": "Project-management owner", "blocking": false}
  ],
  "decisions": [
    {"id": "D1", "decision": "Use stable workflow codes for business startup.", "reason": "Business modules must not depend on mutable database IDs.", "source": "User approval."},
    {"id": "D2", "decision": "Persist lifecycle events and deliver at least once with idempotent consumers.", "reason": "Workflow completion must not be lost during consumer or process failure.", "source": "User approval after latest main inspection."},
    {"id": "D3", "decision": "Put compact inbox and done summaries on the dashboard.", "reason": "Approvers need a unified workbench while retaining full workflow pages.", "source": "User approval."},
    {"id": "D4", "decision": "Create a separate ccb-attachment platform module.", "reason": "Persistent business ownership is distinct from temporary preview.", "source": "User approval of the recommended architecture."},
    {"id": "D5", "decision": "Use replaceable frontend Mock projects and defer formal server project authorization.", "reason": "Project management is under separate implementation.", "source": "User instruction."},
    {"id": "D6", "decision": "Internal signatures use current authenticated identity and explicit click confirmation without password re-entry.", "reason": "Matches the requested internal approval strength and avoids external authentication scope.", "source": "User explicit confirmation."},
    {"id": "D7", "decision": "Reuse the existing SystemNotificationPublisher.", "reason": "Latest main already has a public idempotent notification contract.", "source": "Repository inspection."}
  ],
  "risks": [
    {"risk": "Two workflow execution paths emit inconsistent events.", "impact": "Business state can diverge.", "mitigation": "Use one event factory and contract suite for both engines."},
    {"risk": "At-least-once delivery causes duplicate or stale transitions.", "impact": "Business state may repeat or regress.", "mitigation": "Stable event IDs, business-round checks, state preconditions, and unique consumer records."},
    {"risk": "Session-confirm signatures are mistaken for legal digital signatures.", "impact": "Governance or compliance misunderstanding.", "mitigation": "Label the feature as internal confirmation and retain digest-bound audit evidence."},
    {"risk": "Attachment policies are absent or incorrect.", "impact": "Files become inaccessible or exposed.", "mitigation": "Fail closed, test each business policy, and never expose object keys."},
    {"risk": "Mock project values are trusted as authorization.", "impact": "Cross-project data exposure.", "mitigation": "Treat them as display only and keep formal project authorization as an explicit dependency."}
  ],
  "approval": {
    "status": "approved",
    "evidence": "User confirmed the landed platform design, selected current-session click confirmation for internal signatures, and directed platform-first delivery.",
    "confirmed_at": "2026-08-14T10:53:02+08:00"
  }
}
