{
  "task_id": "REQ-20260904-062",
  "system_boundary": "web/src/modules/release/components/ReleaseDrillPlanView.vue and release-operations.css",
  "inputs": ["ReleasePlanDto.items", "timelineType", "projectId", "canManage"],
  "transform": "filter by timeline type, sort by seqNo/start, group by seqNo, derive stage range and axis labels",
  "outputs": ["stage columns", "instruction cards", "timeline axis"],
  "sensors": ["frontend build", "git diff check", "browser screenshots", "console errors", "viewport overflow"],
  "disturbances": ["missing seqNo", "missing dates", "long text", "empty items", "narrow drawer"],
  "invariants": ["backend contract unchanged", "write controls follow canManage", "page-level overflow absent"]
}
