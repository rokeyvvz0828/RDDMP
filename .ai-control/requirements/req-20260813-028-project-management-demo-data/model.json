{
  "schema_version": 1,
  "plant_boundary": ["mock JSON", "mock allowlist", "local backend startup", "project REST API"],
  "state_variables": ["datasetVersion", "project rows", "plan tree rows", "member and role relations"],
  "sensors": ["JSON parser", "Maven package", "startup log", "project API response"],
  "actuators": ["mock data file", "allowlist entries", "local backend restart"],
  "disturbances": ["stale packaged jar", "database unavailable", "existing rows with same mock IDs"],
  "invariants": ["tenant_id=1", "no production access", "project hierarchy and date bounds remain valid"]
}
