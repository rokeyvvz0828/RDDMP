{
  "schema_version": 1,
  "topic": "req-20260807-012-workflow-demo-data",
  "design": [
    "Use append-only Flyway V30 so the local database receives data during normal startup.",
    "Keep definitions and records tenant-scoped to tenant 1 and use DEMO_ codes with isolated high IDs.",
    "Use the existing admin user as the assignee so the current local login can see inbox and done records.",
    "Keep one draft schema-version-2 definition for designer inspection and use legacy published definitions for runtime-safe local testing."
  ]
}
