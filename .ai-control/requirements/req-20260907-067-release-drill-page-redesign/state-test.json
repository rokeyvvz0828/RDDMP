{
  "description": "Repeatable isolated state tests for original and redesigned drill component.",
  "script": "const fs = require('node:fs');\nconst assert = require('node:assert/strict');\nconst ts = require('./web/node_modules/typescript');\nconst vue = require('./web/node_modules/vue');\nconst { parse } = require('./web/node_modules/@vue/compiler-sfc');\nconst source = fs.readFileSync('web/src/modules/release/components/ReleaseDrillExecutionView.vue','utf8');\nconst descriptor = parse(source).descriptor;\nconst output = ts.transpileModule(descriptor.scriptSetup.content, {\n  compilerOptions: { target: ts.ScriptTarget.ES2022, module: ts.ModuleKind.ESNext },\n  transformers: { before: [context => root => ts.visitNode(root, function visit(node) {\n    if (ts.isImportDeclaration(node)) return undefined;\n    return ts.visitEachChild(node, visit, context);\n  })] }\n}).outputText.replace('export {};','');\nconst step = {id:101,projectId:7,drillRoundId:11,seqNo:2,stepName:'Synthetic step',ownerId:21,ownerName:'Test member',plannedStart:'2026-09-07T09:00:00',plannedEnd:'2026-09-07T10:00:00',status:'PENDING',resultContent:'Synthetic result',description:'Synthetic description',rowVersion:3};\nconst round = {id:11,projectId:7,roundNo:1,roundName:'Synthetic round A',plannedAt:'2026-09-07T09:00:00',status:'PLANNED',resultContent:'Round result',releasePlanId:31,releasePlanName:'Test plan',environmentId:41,environmentName:'Test environment',rowVersion:4,steps:[step]};\nconst other = {...round,id:12,roundNo:2,roundName:'Synthetic round B',steps:[]};\nfunction setup(projectId=7,manage=true) {\n  const calls=[],messages=[],hooks={};\n  let records=structuredClone([round,other]).map(r=>({...r,projectId}));\n  let fail=null, confirm='accept', waiting=null, listWaiting=null;\n  const props={projectId};\n  const wrap=data=>({data:{data}});\n  const env={ref:vue.ref,reactive:vue.reactive,computed:vue.computed,onMounted:f=>{hooks.mount=f;},onBeforeUnmount:f=>{hooks.unmount=f;},defineProps:()=>props,useAuthStore:()=>({hasPermission:()=>manage}),\n    apiErrorMessage:(_,text)=>text,ElMessage:{success:m=>messages.push(['success',m]),error:m=>messages.push(['error',m]),warning:m=>messages.push(['warning',m])},ElMessageBox:{confirm:async()=>{calls.push(['confirm']);if(confirm!=='accept')throw confirm;}},\n    listReleaseDrills:async id=>{calls.push(['list',id]);const snapshot=structuredClone(records),hold=listWaiting;if(hold)await hold;if(fail)throw fail;return wrap(snapshot);},\n    listReleasePlans:async id=>{calls.push(['plans',id]);return wrap([{id:31,planName:'Test plan',planCode:'SYNTHETIC'}]);},\n    listReleaseDrillEnvironments:async id=>{calls.push(['environments',id]);return wrap([{id:41,environmentName:'Test environment'}]);},\n    listReleaseOperationMemberOptions:async id=>{calls.push(['members',id]);return wrap([{userId:21,displayName:'Test member',username:'synthetic'}]);}\n  };\n  for(const [key,name,isStep,isCreate] of [\n    ['createReleaseDrill','createRound',false,true],['updateReleaseDrill','updateRound',false,false],\n    ['createReleaseDrillStep','createStep',true,true],['updateReleaseDrillStep','updateStep',true,false]\n  ])env[key]=async(...args)=>{calls.push([name,...args]);if(waiting)await waiting;if(fail)throw fail;const data=args.at(-1);return wrap(isStep?{...step,...data,id:isCreate?102:args[2],projectId:args[0],drillRoundId:args[1],seqNo:data.seqNo||3}:{...round,...data,id:isCreate?13:args[1],projectId:args[0],steps:[]});};\n  env.deleteReleaseDrill=async(...args)=>{calls.push(['deleteRound',...args]);if(fail)throw fail;return wrap(null);};\n  env.deleteReleaseDrillStep=async(...args)=>{calls.push(['deleteStep',...args]);if(fail)throw fail;return wrap(null);};\n  const api=new Function(...Object.keys(env),output+'\\nreturn {rounds,plans,environments,members,selectedId,selectedRound,roundForm,stepForm,roundDialog,stepDrawer,saving,stepSaving,error,forbidden,loading,canManage,load,openRound,saveRound,removeRound,openStep,saveStep,removeStep,closeRound:typeof closeRound===\"undefined\"?null:closeRound,closeStep:typeof closeStep===\"undefined\"?null:closeStep};')(...Object.values(env));\n  return {api,calls,messages,hooks,props,setManage:v=>{manage=v;},setListWaiting:v=>{listWaiting=v;},setRecords:r=>{records=r;},setFail:v=>{fail=v;},setConfirm:v=>{confirm=v;},setWaiting:v=>{waiting=v;}};\n}\n(async()=>{\n  const checks=[]; const check=(name,fn)=>{fn();checks.push(name);};\n  let t=setup(),a=t.api;await a.load();\n  check('Load calls project-scoped existing APIs',()=>{assert.deepEqual(t.calls.map(c=>c[1]),[7,7,7,7]);assert.equal(a.selectedRound().id,11);});\n  a.selectedId.value=12;const n=t.calls.length;\n  check('Selection switches only local current round',()=>{assert.equal(a.selectedRound().roundName,'Synthetic round B');assert.equal(t.calls.length,n);});\n  await a.load();check('Refresh retains an existing selection',()=>assert.equal(a.selectedRound().id,12));\n  t.setRecords([]);await a.load();check('Empty rounds clear selection',()=>{assert.equal(a.selectedRound(),undefined);assert.equal(a.selectedId.value,undefined);});\n  t.setRecords([round,other]);await a.load();a.openRound(round);a.roundForm.roundName='Changed';\n  check('Round form never mutates source',()=>assert.equal(round.roundName,'Synthetic round A'));\n  a.openRound();a.roundForm.roundName=' ';await a.saveRound();\n  check('Blank round name prevents submit',()=>assert.equal(t.calls.filter(c=>c[0]==='createRound').length,0));\n  a.roundForm.roundName='Synthetic new';a.roundForm.releasePlanId=0;await a.saveRound();\n  check('Round requires a plan',()=>assert.equal(t.calls.filter(c=>c[0]==='createRound').length,0));\n  a.roundForm.releasePlanId=31;a.roundForm.environmentId=0;await a.saveRound();\n  check('Round requires an environment',()=>assert.equal(t.calls.filter(c=>c[0]==='createRound').length,0));\n  a.roundForm.environmentId=41;await a.saveRound();\n  check('Create selects new round and closes dialog',()=>{assert.equal(a.selectedId.value,13);assert.equal(a.roundDialog.value,false);assert.equal(t.calls.find(c=>c[0]==='createRound')[1],7);});\n  a.openRound(round);await a.saveRound();\n  check('Round update preserves id/version and fields',()=>{const c=t.calls.find(c=>c[0]==='updateRound');assert.equal(c[1],7);assert.equal(c[2],11);assert.equal(c[3].rowVersion,4);assert.equal(c[3].resultContent,'Round result');});\n  t=setup();a=t.api;await a.load();a.openStep(step);a.stepForm.stepName='Changed step';\n  check('Step edit uses copied fields and combined range',()=>{assert.equal(step.stepName,'Synthetic step');assert.deepEqual(a.stepForm.range,[step.plannedStart,step.plannedEnd]);});\n  await a.saveStep();\n  check('Step update preserves round/id/version and range payload',()=>{const c=t.calls.find(c=>c[0]==='updateStep');assert.deepEqual(c.slice(1,4),[7,11,101]);assert.equal(c[4].rowVersion,3);assert.equal(c[4].plannedStart,step.plannedStart);assert.equal(c[4].plannedEnd,step.plannedEnd);assert.equal(c[4].description,step.description);});\n  a.openStep();await a.saveStep();\n  check('Empty step name prevents create',()=>assert.equal(t.calls.filter(c=>c[0]==='createStep').length,0));\n  a.stepForm.stepName='First';a.stepForm.seqNo=1;await a.saveStep();\n  check('New step inserted in sequence order',()=>assert.deepEqual(a.selectedRound().steps.map(s=>s.seqNo),[1,2]));\n  t.setFail(Error('synthetic'));a.openRound(round);await a.saveRound();\n  check('Round save failure retains dialog and input',()=>{assert.equal(a.roundDialog.value,true);assert.equal(a.roundForm.roundName,round.roundName);assert.equal(a.saving.value,false);});\n  a.roundDialog.value=false;a.openStep(step);await a.saveStep();\n  check('Step save failure retains drawer and input',()=>{assert.equal(a.stepDrawer.value,true);assert.equal(a.stepForm.description,step.description);assert.equal(a.stepSaving.value,false);});\n  t.setFail(null);t.setConfirm('cancel');const errors=t.messages.filter(m=>m[0]==='error').length;await a.removeRound(round);await a.removeStep(step);\n  check('Delete cancel does not write or report failure',()=>{assert.equal(t.calls.filter(c=>c[0].startsWith('delete')).length,0);assert.equal(t.messages.filter(m=>m[0]==='error').length,errors);});\n  t.setConfirm('close');await a.removeRound(round);await a.removeStep(step);\n  check('Delete close does not write',()=>assert.equal(t.calls.filter(c=>c[0].startsWith('delete')).length,0));\n  t.setConfirm('accept');await a.removeStep(step);await a.removeRound(other);\n  check('Confirmed deletions preserve parameters and round fallback',()=>{assert.deepEqual(t.calls.find(c=>c[0]==='deleteStep'),['deleteStep',7,11,101,3]);assert.deepEqual(t.calls.find(c=>c[0]==='deleteRound'),['deleteRound',7,12,4]);assert.equal(a.selectedId.value,11);});\n  t=setup(8,false);a=t.api;await a.load();check('Read-only permission and other project are distinct',()=>{assert.equal(a.canManage(),false);assert.equal(t.calls[0][1],8);});\n  t.setFail({response:{status:403}});await a.load();check('403 stays separate from generic failure',()=>assert.equal(a.forbidden.value,true));\n  t.setFail(Error('network'));await a.load();check('Load failure clears loading and allows retry',()=>{assert.equal(a.forbidden.value,false);assert.ok(a.error.value);assert.equal(a.loading.value,false);});\n  t.setFail(null);await a.load();check('Retry clears load error',()=>assert.equal(a.error.value,''));\n  check('Original overlay types and combined range retained',()=>{assert.equal((descriptor.template.content.match(/<el-dialog\\b/g)||[]).length,1);assert.equal((descriptor.template.content.match(/<el-drawer\\b/g)||[]).length,1);assert.ok(descriptor.template.content.includes('type=\"datetimerange\"'));assert.ok(!descriptor.template.content.includes('v-html'));});\n\n  function deferred() {let resolve;const promise=new Promise(r=>{resolve=r;});return {promise,resolve};}\n  t=setup();a=t.api;await a.load();a.openStep(step);a.selectedId.value=12;await a.saveStep();\n  check('Step form keeps original round after selection changes',()=>{assert.equal(t.calls.find(c=>c[0]==='updateStep')[2],11);assert.equal(a.selectedRound().steps.length,0);});\n  a.selectedId.value=11;a.openStep();a.stepForm.stepName='No schedule';a.stepForm.range=null;await a.saveStep();\n  check('Cleared optional range submits without failure',()=>{const data=t.calls.find(c=>c[0]==='createStep').at(-1);assert.equal(data.plannedStart,undefined);assert.equal(data.plannedEnd,undefined);assert.equal(a.stepDrawer.value,false);});\n  t=setup();a=t.api;await a.load();a.openRound();const pendingRound=deferred();t.setWaiting(pendingRound.promise);\n  const roundSave=a.saveRound();await a.saveRound();let closed=0;a.closeRound();a.closeRound(()=>closed++);a.openRound(other);\n  check('Round saving blocks duplicate writes and dismissal/reopen',()=>{assert.equal(t.calls.filter(c=>c[0]==='createRound').length,1);assert.equal(a.roundDialog.value,true);assert.equal(closed,0);assert.notEqual(a.roundForm.roundName,other.roundName);});\n  pendingRound.resolve();await roundSave;t.setWaiting(null);a.openRound();a.closeRound();\n  check('Idle round cancel closes dialog',()=>assert.equal(a.roundDialog.value,false));\n  a.selectedId.value=11;a.openStep(step);const pendingStep=deferred();t.setWaiting(pendingStep.promise);const stepSave=a.saveStep();await a.saveStep();a.closeStep();a.closeStep(()=>closed++);a.openStep();\n  check('Step saving blocks duplicate writes and dismissal/reopen',()=>{assert.equal(t.calls.filter(c=>c[0]==='updateStep').length,1);assert.equal(a.stepDrawer.value,true);assert.equal(closed,0);assert.equal(a.stepForm.stepName,step.stepName);});\n  pendingStep.resolve();await stepSave;t.setWaiting(null);a.openStep();a.closeStep();\n  check('Idle step cancel closes drawer',()=>assert.equal(a.stepDrawer.value,false));\n  t=setup(7,false);a=t.api;await a.load();a.openRound();a.openStep(step);Object.assign(a.roundForm,{releasePlanId:31,environmentId:41,roundName:'Denied'});a.stepForm.stepName='Denied';await a.saveRound();await a.saveStep();await a.removeRound(round);await a.removeStep(step);\n  check('Read-only handlers cannot open forms or write/delete',()=>{assert.equal(a.roundDialog.value,false);assert.equal(a.stepDrawer.value,false);assert.equal(t.calls.length,4);});\n  t=setup();a=t.api;await a.load();a.openStep(step);a.openRound(round);t.setManage(false);await a.saveRound();await a.saveStep();\n  check('Revoked manage permission blocks open-form submit',()=>assert.equal(t.calls.length,4));\n  t=setup();a=t.api;const stale=deferred();t.setListWaiting(stale.promise);const firstLoad=a.load();t.setRecords([other]);t.setListWaiting(null);await a.load();stale.resolve();await firstLoad;\n  check('Older load response cannot overwrite newer refresh',()=>{assert.deepEqual(a.rounds.value.map(r=>r.id),[12]);assert.equal(a.loading.value,false);});\n  t=setup();a=t.api;const late=deferred();t.setListWaiting(late.promise);const lateLoad=a.load();t.hooks.unmount();late.resolve();await lateLoad;\n  check('Unmount ignores a pending load response',()=>assert.equal(a.rounds.value.length,0));\n  t=setup();a=t.api;await a.load();a.openRound();const leaving=deferred();t.setWaiting(leaving.promise);const leavingSave=a.saveRound();t.hooks.unmount();leaving.resolve();await leavingSave;\n  check('Unmount ignores pending round result and notifications',()=>{assert.equal(a.rounds.value.length,2);assert.equal(t.messages.length,0);});\n  t=setup();a=t.api;await a.load();a.openStep(step);const leavingStep=deferred();t.setWaiting(leavingStep.promise);const leavingStepSave=a.saveStep();t.hooks.unmount();leavingStep.resolve();await leavingStepSave;\n  check('Unmount ignores pending step result and notifications',()=>assert.equal(t.messages.length,0));\n  t=setup();a=t.api;await a.load();const deleting=a.removeRound(round);t.hooks.unmount();await deleting;\n  check('Unmount during confirmation prevents deletion request',()=>assert.equal(t.calls.filter(c=>c[0]==='deleteRound').length,0));\n  t=setup();a=t.api;await a.load();const deletingStep=a.removeStep(step);t.setManage(false);await deletingStep;\n  check('Revoked permission during confirmation prevents step deletion',()=>assert.equal(t.calls.filter(c=>c[0]==='deleteStep').length,0));\n  t=setup();a=t.api;await a.load();a.openStep();a.stepForm.stepName='Deleted round';a.rounds.value=[];\n  await a.saveStep();check('Removed round cannot receive a pending step form',()=>assert.equal(t.calls.filter(c=>c[0]==='createStep').length,0));\n  check('Overlay close protection and mobile layout hooks are wired',()=>{for(const text of [':before-close=\"closeRound\"',':before-close=\"closeStep\"',':close-on-press-escape=\"!saving\"',':close-on-click-modal=\"!stepSaving\"','v-model=\"selectedId\"'])assert.ok(descriptor.template.content.includes(text));assert.ok(descriptor.styles[0].content.includes('@media (max-width: 760px)'));assert.ok(descriptor.styles[0].content.includes('overflow-x: auto'));});\n\n  console.log(JSON.stringify({passed:checks.length,checks,limitations:['Synthetic state tests only; no real browser, server writes or independent acceptance']},null,2));\n})().catch(e=>{console.error(e);process.exitCode=1;});"
}
