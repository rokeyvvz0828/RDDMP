# RDDMP 移动端适配工程手册

本手册是 RDDMP 前端移动端适配的正式工程依据，适用于所有新增、改造和评审中的业务页面。它描述的是可复用的设计目标、实现边界、组件规则和验收门禁，不是某个页面的临时样式说明。

## 1. 适用范围

以下内容必须遵循本手册：

- `web/src/views/` 下的业务页面、列表页、详情页、弹框和抽屉。
- `web/src/modules/` 下的交付示范中心和后续业务模块。
- `web/src/components/ui/` 下影响页面布局、表单、列表、弹层、通知和文件预览的公共组件。
- 流程图、图表、甘特图、组织树、时间线等固定格式或高信息密度组件。
- 工作台、导航、页签、用户操作区等应用壳层。

纯桌面内部工具可以申请例外，但需求设计或当前 `.ai-control` 任务记录必须说明目标用户、无法适配的原因、仍复用的公共能力和例外边界。

## 2. 适配目标

移动端适配不是把桌面页面缩小，而是让手机用户在不放大页面、不依赖整页左右拖动的情况下完成核心任务。

### 2.1 必达目标

1. 页面在 `375x812`、`390x844` 和 `430x932` 视口中可用。
2. 页面主体、弹框、抽屉、按钮、标签和文字不得产生非预期横向溢出。
3. 核心对象、状态、主操作在首屏或局部滚动范围内可识别、可触达。
4. 桌面端信息密度和交互习惯保持稳定，移动端只改变布局和操作层级，不改变业务规则、权限和接口契约。
5. 长表格、流程图和图表必须有明确的局部滚动或重排策略，不能把整个页面变成无边界的横向画布。
6. 加载、空数据、失败、无权限、只读、提交中和重复提交状态在手机端仍然可见且不遮挡内容。

### 2.2 验收视口

最低验证集合：

| 视口 | 用途 |
| --- | --- |
| `375x812` | 小屏手机，检查最紧张的按钮、标题和两列信息项 |
| `390x844` | 常见手机，检查核心业务流程和弹框 |
| `430x932` | 大屏手机，检查内容是否出现过度留白或固定宽度假设 |
| `1280x800` 或更大 | 检查桌面布局没有被移动端规则破坏 |

## 3. 适配分层

移动端问题必须按以下边界处理：

### 3.1 平台底层适配

由应用壳层和公共样式负责：

- `body`、应用壳层和主内容区不得使用阻塞手机的固定最小宽度。
- 侧边导航在手机端隐藏，使用抽屉或菜单按钮访问。
- 顶部导航压缩高度，品牌、当前页面、通知、用户入口保持可用。
- 页签、通知抽屉、普通抽屉和弹框限制在视口范围内。
- 弹层内容采用“标题固定、内容局部滚动、操作区可达”的结构。
- 全局只处理滚动边界和尺寸约束，不负责理解业务字段。

### 3.2 业务组件适配

由业务页面或领域组件负责：

- 桌面表格是否切换为移动卡片。
- 字段优先级、信息分组、操作层级和状态表达。
- 流程图的横向/纵向布局、节点尺寸和缩放。
- 表单字段从多列变为单列、分步表单的步骤展示和校验提示。
- 图表、甘特图和时间线的容器比例、标签密度和局部滚动。

底层 CSS 不得通过强制缩小字体、裁剪内容或给所有组件加统一横向滚动来掩盖业务布局问题。

## 4. 页面和容器规则

### 4.1 宽度与溢出

- 页面根节点使用 `min-width: 0`，业务容器使用 `width: 100%` 和可收缩的 grid/flex 子项。
- 不在 `body`、`.app-shell`、`.app-main` 或页面根节点上设置手机不可用的固定 `min-width`。
- `overflow-x: auto` 只能作用于明确的数据表、流程画布或图表容器，不能作用于整个页面。
- 固定格式内容必须定义稳定尺寸、最大尺寸或 `aspect-ratio`，避免内容加载后改变布局。
- 长中文、业务单号、流程编码和外部标识使用 `overflow-wrap: anywhere` 或明确的省略策略。

### 4.2 页面结构

业务页面优先采用以下结构：

1. `UiPageHeader`：标题和主操作。
2. `UiToolbar`：检索、筛选和刷新。
3. 主内容：桌面表格或移动卡片/列表。
4. 加载、空、失败和结果数量状态。

页面不能把所有操作按钮直接平铺在标题、卡片头部或一行工具栏中。高频主操作和低频危险操作必须有层级差异。

## 5. 列表和数据表

### 5.1 选择呈现模式

| 数据形态 | 桌面端 | 移动端 |
| --- | --- | --- |
| 2-4 个核心字段、1-2 个操作 | 表格或列表 | 可保留紧凑列表，也可使用卡片 |
| 5 个以上字段 | `UiDataTable` 表格 | 优先改为信息卡片 |
| 需要逐列比较的财务、统计数据 | 表格 | 保留局部横向滚动，滚动区域必须明确 |
| 待办、工单、流程实例、业务对象 | 表格 | 业务卡片，避免整页左右滑动 |
| 操作超过 3 个 | 操作列按现有桌面规则 | 主操作 + 高频操作 + “更多操作”菜单 |

### 5.2 移动卡片结构

移动业务卡片按固定顺序组织：

1. **身份区**：业务单号、名称或主键；状态放在右侧且按内容宽度显示。
2. **事实区**：使用两列信息项；每项包含字段名和字段值。字段值过长时换行，不覆盖相邻内容。
3. **操作区**：顶部边框分隔；主操作、审批高频操作、低频操作分别分组。
4. **补充区**：审批意见、风险、进度或更新时间按需展示，不能挤压身份区。

状态标签必须使用 `UiStatusTag` 或等价公共状态组件，并使用 `justify-self: start`、`width: fit-content` 等规则避免被网格拉伸成输入框。

### 5.3 操作层级

- 主操作：查看详情、打开流程图、编辑等，最多 1 个，使用明确的按钮样式。
- 高频操作：例如审批中的“同意”，最多 1-2 个，使用语义颜色但不与主操作争夺位置。
- 低频或危险操作：拒绝、退回、删除、取消发布、加签等，进入“更多操作”菜单。
- 同一张手机卡片不直接平铺 4 个以上操作按钮。
- 操作菜单必须保留原有权限、禁用条件、确认提示和错误反馈，不得因为移动端收起菜单而绕过逻辑。
- 抄送、只读和已完成记录不应显示无效的审批按钮，应提供详情或记录说明。

### 5.4 表格滚动例外

只有以下情况允许移动端局部横向滚动：

- 用户确实需要逐列比较，改成卡片会丢失关键关系。
- 表格是导出前的预览或专业数据网格。
- 流程节点明细、审计字段等次级明细无法在卡片中保持可读性。

此时必须满足：滚动区域有边界、页面本身不横向滚动、首列或关键列尽量保持可见、表头和数据列宽稳定，并在验收记录中说明原因。

## 6. 弹框、抽屉和详情页

- 手机端弹框宽度使用 `calc(100vw - 24px)` 或等价约束，不能沿用桌面固定宽度。
- 弹框内容区使用视口相关最大高度，内容纵向滚动；标题和底部操作不能被内容滚动吞掉。
- 摘要信息移动端使用两列或单列自适应，长标题和业务单号允许换行。
- 表单抽屉在手机端接近全宽，但保留安全边距；字段默认单列，操作区始终可达。
- 详情弹框不应同时承载过多低频字段。可以将审计、明细表、附件和历史记录放在后续区块，但不能通过缩小字号解决信息过载。
- 弹层内部的表格、流程图和图表只能在自己的容器中滚动，不能让弹层整体产生不可理解的横向拖动。

## 7. 流程图、时间线和审批

### 7.1 流程图

- 桌面端默认保留横向流程布局，适合流程比较和设计操作。
- 手机端只读查看自动采用纵向布局；开始、审批、网关、结束节点按真实尺寸重新排版。
- 流程节点尺寸、节点间距和画布高度必须使用移动端参数重新计算，不能只依赖桌面位置缩放。
- 流程数据加载、画布渲染和节点尺寸测量完成后再执行 `fitView` 或等价缩放，防止末节点被截断。
- 画布不允许撑宽页面；缩放控件、提示文案和节点状态不能覆盖节点内容。
- 流程图下方的节点状态表可以局部滚动，但流程图本身必须在手机宽度内完整可理解。

### 7.2 审批操作

- 审批详情保留业务单号、流程名称、当前节点、处理人、状态和进入时间。
- “查看详情”是主操作；“同意”是待办高频操作；拒绝、退回、加签、抄送进入更多操作。
- 危险或不可逆操作必须保留确认提示、审批意见和服务端权限校验。
- 状态颜色必须有明显语义差异，同时保留文字，不得只依赖颜色。

## 8. 表单、图表和固定格式组件

### 8.1 表单

- `760px` 以下默认从多列变为单列，标签、输入控件和错误信息垂直排列。
- 输入框、选择器、日期选择器和上传控件宽度不得超出父容器。
- 分步表单在手机端可使用横向步骤滚动，但当前步骤、错误步骤和下一步操作必须明确可见。
- 校验信息在字段附近展示，不用弹出提示替代关键字段错误。
- 提交、取消、下一步和返回按钮需要稳定位置和足够触控面积，避免固定宽度导致文字被截断。

### 8.2 图表和甘特图

- 图表容器必须定义宽度、高度或 `aspect-ratio`，加载前后不能跳动。
- 移动端优先减少标签密度、图例数量和装饰元素，保证趋势、数值和状态可读。
- 需要逐项查看的图表允许容器内横向滚动，但不允许页面整体横向滚动。
- 甘特图应提供时间轴局部滚动，任务名称和关键状态不能与时间轴重叠。
- 图表无数据、加载失败和数据为空时提供明确状态，不显示空白画布。

## 9. 主题、可访问性和状态

- 使用 `--page-bg`、`--panel-bg`、`--line`、`--text`、`--muted`、`--brand`、`--success`、`--warning`、`--danger` 等语义变量，不在业务页面硬编码主题色。
- 颜色只表达语义，不作为唯一信息来源；状态、按钮和图表同时保留文字或图形线索。
- 图标按钮必须提供 `title` 或可访问名称；“更多操作”菜单要有明确名称。
- 禁用、加载中、空数据和失败状态不能因为移动端隐藏桌面表格而丢失。
- 文字、标签和按钮必须在 375px 宽度下完整容纳，不能通过负字距或极小字号强行塞入一行。

## 10. 实施检查清单

开发前：

- [ ] 已阅读本手册、`web/src/AGENTS.md` 和 `docs/integration/frontend-ui-contract.md`。
- [ ] 已检查 `web/src/modules/delivery-showcase/`，确认复用的列表、表单、弹框、流程图或图表模式。
- [ ] 已明确桌面模式、移动模式、局部滚动区域和操作层级。
- [ ] 已列出加载、空、失败、无权限、只读、提交中和重复提交状态。

编码中：

- [ ] 页面根节点没有新增阻塞手机的固定最小宽度。
- [ ] 业务列表已选择表格、卡片或局部横向滚动，并记录选择理由。
- [ ] 主操作、高频操作和低频操作有明确层级。
- [ ] 弹框、抽屉、流程画布和图表的滚动边界清晰。
- [ ] 未修改业务逻辑、权限、接口契约或数据结构来实现样式适配。

完成前：

- [ ] 已执行 `git diff --check`。
- [ ] 已执行 `npm --prefix web run build`。
- [ ] 已在 `375x812`、`390x844`、`430x932` 和桌面视口检查。
- [ ] 已检查 `document.documentElement.scrollWidth` 与视口宽度，确认不存在页面级横向溢出。
- [ ] 已点击主操作、更多操作、危险操作确认、返回和关闭弹层，确认操作可达且上下文保留。
- [ ] 已检查浅色/深色主题、长业务单号、空列表、失败提示和加载状态。
- [ ] 已在任务交接或 `.ai-control` 观测记录中写入实际验证视口、路径、结果和未决风险。

## 11. 例外和回退

不能满足本手册时，必须在需求设计或当前 `.ai-control` 任务记录中写明：

1. 无法适配的业务原因和目标设备范围。
2. 仍然复用的公共组件、主题变量和交互模式。
3. 例外样式的最小适用边界。
4. 对页面级横向滚动、可达性和数据可读性的影响。
5. 回退方式和后续补齐计划。

移动端样式出现回归时，优先回退业务组件的专项适配，不应直接删除平台底层适配；底层变更必须同时回归导航、页签、通知、抽屉、弹框和交付示范中心。
